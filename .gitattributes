# 仓库内统一使用 LF，避免 Windows / macOS / Linux 之间来回转换产生的无意义 diff
* text=auto eol=lf

# 二进制文件不做行尾转换
*.png binary
*.jpg binary
*.ico binary
*.wasm binary
*.tar binary

# 生成产物不参与语言统计
dist/** linguist-generated=true
package-lock.json linguist-generated=true
src/data/problems/*.json linguist-generated=true
