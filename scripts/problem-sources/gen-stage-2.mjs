/**
 * 生成 src/data/problems/stage-2.json
 * （阶段二 · 运算符与条件判断）
 *
 * 用脚本生成 JSON 而不是手写，避免 Markdown 里的换行/引号/反引号转义出错。
 * 生成后请运行：node scripts/validate-problems.mjs --stage=2
 */
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const outDir = path.join(path.dirname(fileURLToPath(import.meta.url)), '..', '..', 'src', 'data', 'problems');

const starter = (body) => `#include <iostream>
using namespace std;

int main() {
${body}
    return 0;
}
`;

const stage = {
  stage: 2,
  title: '阶段二 · 让程序学会判断',
  subtitle: '运算符与条件分支',
  summary:
    '程序只会从上往下一条条执行是不够的，它还需要"看情况做决定"。本阶段先学会用 `+ - * / %` 做运算、用 `> < >= <= == !=` 比较大小，再用 `&& || !` 把条件组合起来，最后用 `if / else if / else`、三目运算符和 `switch` 让程序走进不同的分支。这些就是"会思考"的程序的开始。',
  problems: [
    // ---------------------------------------------------------------- p1
    {
      id: 's2-p1',
      title: '奇数还是偶数',
      difficulty: '入门',
      knowledge_point: '取余与奇偶判断',
      description: `### 题目背景

生活中我们常常要给数字分类：门牌号是单号还是双号、座位是奇数排还是偶数排……

在数学里，**能被 2 整除**的整数叫**偶数**，不能被 2 整除的叫**奇数**。

C++ 提供了一个专门求余数的运算符 \`%\`（读作"取余"或"取模"），它算出的是"除完以后剩下多少"：

\`\`\`
7 % 2 的结果是 1   // 7 除以 2，商 3 余 1
8 % 2 的结果是 0   // 8 除以 2，商 4 余 0
\`\`\`

所以只要一个数除以 2 的余数是 0，它就是偶数。

### 任务

读入一个整数 \`n\`，判断它是奇数还是偶数。

### 本关新知识：取余 \`%\` 与 \`if / else\`

**先认识取余运算符 \`%\`**：它算的是"除完以后剩下多少"，可以把它想成**分糖果**——7 颗糖 2 个人平分，每人拿 3 颗，最后剩下 1 颗，这个"剩下的 1"就是 \`7 % 2\` 的结果：

\`\`\`
7 % 2   // 结果是 1：7 除以 2，商 3 余 1
8 % 2   // 结果是 0：正好分完，一点不剩
\`\`\`

**再认识 \`if / else\`**：这是让程序"看情况做决定"的最基本句型，读作"**如果……就……否则……**"：

\`\`\`
if (条件) {
    // 条件成立时，执行这里的语句
} else {
    // 条件不成立时，执行这里的语句
}
\`\`\`

小括号 \`( )\` 里放**条件**（一个能回答"是 / 不是"的问题），大括号 \`{ }\` 里放**要执行的语句**。本题把这两样拼起来，就是完整的答案：

\`\`\`cpp
int n;
cin >> n;                     // 读入要判断的整数
if (n % 2 == 0) {             // 条件：n 除以 2 的余数等于 0 吗？
    cout << "偶数" << endl;    // 是 —— 它正好能被 2 整除
} else {
    cout << "奇数" << endl;    // 不是 —— 剩下的一律算奇数
}
\`\`\`

| 要记住的点 | 说明 |
| --- | --- |
| \`%\` 是取余，不是百分号 | \`n % 2\` 得到的是余数，只能用在整数之间 |
| \`==\` 才是"等于" | 写一个 \`=\` 是赋值，会把 \`n\` 的值改掉，意思完全不同 |
| 条件必须待在 \`( )\` 里 | 写成 \`if n % 2 == 0\` 少了括号，直接编译报错 |
| 条件后面不要写分号 | \`if (...);\` 会让 \`if\` 变成一句空话，两条分支都会执行 |
| \`{ }\` 要成对出现 | 少一个右括号时，报错行号常常指向很后面，很难找 |

### 输入格式

一行，一个整数 \`n\`（\`-1000 ≤ n ≤ 1000\`）。

### 输出格式

如果 \`n\` 是偶数，输出 \`偶数\`；否则输出 \`奇数\`。输出完要换行。

### 样例

**输入**

\`\`\`
7
\`\`\`

**输出**

\`\`\`
奇数
\`\`\`

### 说明

- \`0\` 是偶数（0 除以 2 余 0）。
- 负数同样分奇偶：\`-3\` 是奇数，\`-8\` 是偶数。

### 小贴士

- 判断条件写成 \`n % 2 == 0\`，条件成立就是偶数。
- C++ 里一个等号 \`=\` 表示"赋值"（把右边存进左边），两个等号 \`==\` 才是"判断相等"，千万别写错。
- \`%\` 只能用在整数之间，\`7.5 % 2\` 是写不出来的。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| \`if (n % 2 = 0)\` | 编译报错：\`=\` 是赋值，而"算式"不是能装东西的变量 | \`if (n % 2 == 0)\` |
| 用 \`if (n % 2 == 1)\` 判断奇数 | \`-3 % 2\` 等于 \`-1\` 而不是 \`1\`，负数会被判成偶数 | 只判断偶数 \`n % 2 == 0\`，其余交给 \`else\` |
| \`if (n % 2 == 0);\` 后面才写 \`{ }\` | 多出的分号让 \`if\` 空转，两条分支都会执行，可能输出两行 | 条件后面不写分号，直接接 \`{\` |
| \`cout << "偶数" << ;\` | 报错 \`expected expression\`：\`<<\` 后面必须有东西 | \`cout << "偶数" << endl;\` |
| 用中文引号 \`“偶数”\` | 编译器不认识全角引号 | 用英文双引号 \`"偶数"\` |`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 用 n % 2 判断奇偶，再分别输出 "偶数" 或 "奇数"',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    if (n % 2 == 0) {\n        cout << "偶数" << endl;\n    } else {\n        cout << "奇数" << endl;\n    }',
      ),
      test_cases: [
        { input: '7\n', expected_output: '奇数\n' },
        { input: '0\n', expected_output: '偶数\n' },
        { input: '-3\n', expected_output: '奇数\n' },
        { input: '-8\n', expected_output: '偶数\n' },
        { input: '1000\n', expected_output: '偶数\n' },
      ],
      hints: [
        '判断条件就是 `n % 2 == 0`：余数为 0 就是偶数。',
        '把判断放进 `if (...) { ... } else { ... }` 里，两条分支各输出一个词。',
        '负数不用担心：`-3 % 2` 的结果是 `-1`，但它不等于 0，所以照样会被判成奇数。',
      ],
    },

    // ---------------------------------------------------------------- p2
    {
      id: 's2-p2',
      title: '三个数中的最大值',
      difficulty: '简单',
      knowledge_point: '最大值与逻辑运算',
      description: `### 题目背景

三个朋友比身高，想知道谁最高。程序要做的，就是把几种可能的情况都想到。

我们学过最简单的条件语句：

\`\`\`
if (条件) {
    // 条件成立时做这件事
} else {
    // 条件不成立时做这件事
}
\`\`\`

要比较的对象变多时，可以**嵌套**使用 \`if\`（在 \`if\` 里面再写 \`if\`），也可以用逻辑运算符把好几个条件拼成一个：

- \`&&\`（并且）：两边都成立，整个条件才成立，例如 \`a > b && a > c\`
- \`||\`（或者）：两边有一个成立，整个条件就成立，例如 \`a > b || a > c\`
- \`!\`（取反）：把成立变不成立、不成立变成立，例如 \`!(a > b)\`

### 任务

读入三个整数 \`a\`、\`b\`、\`c\`，输出其中的最大值。

### 本关新知识：逻辑运算符与 \`else if\` 多分支

要判断"a 是不是三个数里最大的"，我们需要一次问两个问题：**a 比 b 大吗？**、**a 比 c 大吗？**。把几个小条件拼成一个大条件，就要用到**逻辑运算符**：

- \`&&\`（并且）：两边**都**成立，整个条件才成立。例如 \`a >= b && a >= c\`
- \`||\`（或者）：两边**有一个**成立，整个条件就成立
- \`!\`（取反）：成立变不成立，不成立变成立，例如 \`!(a > b)\`

读 \`a >= b && a >= c\` 的时候，在心里念成："**a 大于等于 b，并且 a 大于等于 c**"。

一个条件不够用时，就接着写 \`else if\`。程序会**从上往下**一条条检查，命中一条就跳出去，后面的条件连看都不看：

\`\`\`cpp
int a, b, c;
cin >> a >> b >> c;              // 一条语句就能读入三个整数
if (a >= b && a >= c) {          // ① a 是最大的吗？（两个问题都要成立）
    cout << a << endl;
} else if (b >= c) {             // ② 不是 a，那答案只能在 b、c 里
    cout << b << endl;
} else {                         // ③ 前面都不成立，只剩 c 了
    cout << c << endl;
}
\`\`\`

| 要记住的点 | 说明 |
| --- | --- |
| \`&&\` 要写两个符号 | 只写一个 \`&\` 是另一个运算符（按位与），意思完全不同 |
| 用 \`>=\` 而不是 \`>\` | 输入 \`5 5 3\` 时 \`a > b\` 不成立，最大值会被漏掉 |
| 分支从上往下检查 | 命中一条就结束，所以条件要按"最特别 → 最普通"的顺序排 |
| \`else if\` 要连在一起写 | 中间不能插分号；\`else\` 后面不能带条件，带条件的只能写 \`else if\` |
| 最后一个 \`else\` 兜底 | 剩下的情况不用再写条件，写 \`else\` 就行 |

### 输入格式

一行，三个整数 \`a\`、\`b\`、\`c\`（\`-1000000 ≤ a, b, c ≤ 1000000\`），用空格分隔。

### 输出格式

输出一个整数，表示三个数里最大的那个。输出完要换行。

### 样例

**输入**

\`\`\`
3 7 5
\`\`\`

**输出**

\`\`\`
7
\`\`\`

### 说明

- 最大值可能出现不止一次，比如输入 \`5 5 3\`，答案还是 \`5\`，不用说明它出现了几次。

### 小贴士

- 一种写法：先判断"a 是不是最大的"，条件是 \`a >= b && a >= c\`。
- 如果 a 不是最大的，那答案一定在 b 和 c 之间，这时用 \`else if (b >= c)\` 就够了。
- 剩下那种情况不用再写条件，直接 \`else\` 就行。
- 比较时用 \`>=\` 而不是 \`>\`：数字相等时也能得到正确结果。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| \`if (a > b & a > c)\` | 一个 \`&\` 是按位与，判断结果完全不是"两个都成立"，测试点会错 | 用两个符号 \`&&\` |
| \`if (a >= b && a > c)\` 里一半用了 \`>\` | 输入 \`5 5 3\` 这类有相等数字的用例时，最大值会被跳过 | 两处都用 \`>=\`：\`a >= b && a >= c\` |
| \`else (b >= c) { ... }\` | 编译报错：\`else\` 后面不能跟条件 | 写成 \`else if (b >= c) { ... }\` |
| 第一层 \`if\` 忘了写 \`}\` | 后面的 \`else\` 找不到能配对的 \`if\`，报错信息很难懂 | 每个 \`{\` 后面先补一个 \`}\`，再往中间写内容 |
| 条件里写成 \`if (a = b)\` | 这是赋值：a 被改成 b 的值，条件结果完全不是你想的那回事 | 判断相等要用 \`==\` |`,
      starter_code: starter(
        '    int a, b, c;\n    cin >> a >> b >> c;\n    // TODO: 判断谁最大，并输出它\n    // 提示：先想清楚"a 最大"需要满足什么条件（要用到 &&）',
      ),
      solution_code: starter(
        '    int a, b, c;\n    cin >> a >> b >> c;\n    if (a >= b && a >= c) {\n        cout << a << endl;\n    } else if (b >= c) {\n        cout << b << endl;\n    } else {\n        cout << c << endl;\n    }',
      ),
      test_cases: [
        { input: '3 7 5\n', expected_output: '7\n' },
        { input: '10 10 10\n', expected_output: '10\n' },
        { input: '-1 -5 -3\n', expected_output: '-1\n' },
        { input: '-1000000 0 1000000\n', expected_output: '1000000\n' },
        { input: '5 100 -7\n', expected_output: '100\n' },
      ],
      hints: [
        '"a 最大"的条件是 `a >= b && a >= c`，两个都比较过才算数。',
        '第一层 `if` 不成立时，说明 a 不是最大的，答案只能是 b 或 c，再用一个 `if` 比一比即可。',
        '别忘了处理三个数相等的情况，`>=` 能帮你兜住它。',
      ],
    },

    // ---------------------------------------------------------------- p3
    {
      id: 's2-p3',
      title: '成绩等级',
      difficulty: '简单',
      knowledge_point: 'if 多分支阶梯',
      description: `### 题目背景

老师要根据分数给等级。规则有好几条，程序需要从上往下一一检查，命中哪条就走哪条——这就是 \`if / else if / else\` 阶梯：

\`\`\`
if (条件1) {
    // 条件1 成立时走这里
} else if (条件2) {
    // 条件1 不成立，但条件2 成立时走这里
} else {
    // 上面的都不成立时走这里
}
\`\`\`

最重要的一点：程序是**从上往下**检查的，只要有一条命中，后面的条件就**不再检查**了。所以区间要从"最难的"排到"最简单的"。

### 任务

读入一个分数，按下面的规则输出等级：

| 分数区间 | 等级 |
| --- | --- |
| 90 分及以上 | 优秀 |
| 80 ~ 89 分 | 良好 |
| 70 ~ 79 分 | 中等 |
| 60 ~ 69 分 | 及格 |
| 60 分以下 | 不及格 |

### 本关新知识：\`if / else if / else\` 阶梯

分数有好几个区间，"成立 / 不成立"两个出口不够用了，这时候把多个 \`if\` 串成一条**阶梯**：

\`\`\`
if (条件1) {
    // 条件1 成立 → 走这里
} else if (条件2) {
    // 条件1 不成立、但条件2 成立 → 走这里
} else {
    // 上面的都不成立 → 走这里
}
\`\`\`

阶梯的精髓是"**从上往下、命中即停**"：程序先问条件1，成立就执行它的大括号，然后**整条阶梯直接结束**，后面的条件看都不看。

这就带来一个巨大的方便：**第二层的条件不用再写上限**。因为能走到 \`else if (score >= 80)\` 这一行，说明第一层的 \`score >= 90\` 已经不成立了，分数必然小于 90。所以本题只要从高分往低分一路排下去：

\`\`\`cpp
int score;
cin >> score;
if (score >= 90) {                 // 先问最高的那一档
    cout << "优秀" << endl;
} else if (score >= 80) {          // 能走到这里，说明分数已经小于 90
    cout << "良好" << endl;
} else if (score >= 70) {          // 继续往下退一档
    cout << "中等" << endl;
}                                  // 70 分以下的两档，照这个套路补完即可
\`\`\`

| 要记住的点 | 说明 |
| --- | --- |
| 顺序必须从高到低 | 把 \`score >= 60\` 写在最前面，95 分也会被判成"及格" |
| 中间层用 \`else if\` 连写 | 写成一个个独立的 \`if\`，会出现一个分数输出好几行等级的情况 |
| 边界用 \`>=\` | \`score > 90\` 会让 90 分掉进"良好"，而题目要求 90 分算优秀 |
| 最后用 \`else\` 收尾 | 60 分以下只剩这一种可能，不必再写条件 |
| 大括号成对出现 | 阶梯层数多，\`{ }\` 一乱，报错行号就会指到很后面 |

### 输入格式

一行，一个整数 \`score\`（\`0 ≤ score ≤ 100\`）。

### 输出格式

输出等级文字：\`优秀\`、\`良好\`、\`中等\`、\`及格\` 或 \`不及格\`。输出完要换行。

### 样例

**输入**

\`\`\`
95
\`\`\`

**输出**

\`\`\`
优秀
\`\`\`

### 说明

- \`90\` 分算优秀，\`89\` 分算良好；\`60\` 分算及格，\`59\` 分算不及格。边界一定要看仔细。

### 小贴士

- 第一条写 \`if (score >= 90)\`。它不成立时，你其实已经知道分数小于 90 了，后面不必再写 \`score < 90\`。
- 接着写 \`else if (score >= 80)\`、\`else if (score >= 70)\`……最后用 \`else\` 收尾。
- 顺序绝对不能乱：如果把 \`score >= 60\` 写在最前面，90 分也会被判成"及格"。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| 把 \`if (score >= 60)\` 放在第 1 层 | 95 分先命中"及格"，直接输出错误的等级 | 区间从高到低排，最高档放最前面 |
| 中间层写成 \`if\` 而不是 \`else if\` | 各层互不排斥，一个分数可能输出好几行等级 | 中间层统一用 \`else if\` |
| \`else if (90 >= score >= 80)\` | C++ 不支持连写比较，会先算 \`90 >= score\` 得到 0 或 1，条件几乎永远是假 | 拆成两层：\`score >= 90\`、\`score >= 80\` |
| \`if (score > 90)\` | 90 分被判成"良好"，和题目要求的边界不一致 | 用 \`score >= 90\` |
| 只写到 \`else if (score >= 60)\` 就结束了 | 59 分不满足任何条件，一行都不输出 | 末尾补上 \`else { cout << "不及格" << endl; }\` |`,
      starter_code: starter(
        '    int score;\n    cin >> score;\n    // TODO: 用 if / else if / else 判断分数落在哪个区间\n    // 90 分及以上 → "优秀"，往下依次是 "良好"、"中等"、"及格"，其余 "不及格"',
      ),
      solution_code: starter(
        '    int score;\n    cin >> score;\n    if (score >= 90) {\n        cout << "优秀" << endl;\n    } else if (score >= 80) {\n        cout << "良好" << endl;\n    } else if (score >= 70) {\n        cout << "中等" << endl;\n    } else if (score >= 60) {\n        cout << "及格" << endl;\n    } else {\n        cout << "不及格" << endl;\n    }',
      ),
      test_cases: [
        { input: '95\n', expected_output: '优秀\n' },
        { input: '89\n', expected_output: '良好\n' },
        { input: '79\n', expected_output: '中等\n' },
        { input: '60\n', expected_output: '及格\n' },
        { input: '59\n', expected_output: '不及格\n' },
      ],
      hints: [
        '第一个条件是 `score >= 90`，注意是"大于等于"，90 分也要算优秀。',
        '中间的每一层都用 `else if`，因为前面的条件不成立时，你已经知道分数的上限了。',
        '最后一个分支用 `else` 兜底，剩下的分数（60 分以下）都会走到这里。',
      ],
    },

    // ---------------------------------------------------------------- p4
    {
      id: 's2-p4',
      title: '闰年判断',
      difficulty: '简单',
      knowledge_point: '闰年判断与逻辑',
      description: `### 题目背景

地球绕太阳转一圈大约要 365.2422 天，多出来的零头靠"闰年"补上：闰年的二月有 29 天，全年 366 天。

判断闰年的规则是：

1. 能被 4 整除，并且不能被 100 整除；**或者**
2. 能被 400 整除。

把它翻译成 C++ 的一行条件，就是：

\`\`\`
(year % 4 == 0 && year % 100 != 0) || year % 400 == 0
\`\`\`

这里用到了三个新东西：\`&&\`（并且）、\`||\`（或者）、\`!=\`（不等于）。括号非常重要——它是在告诉计算机："先把括号里的两件事一起看，再把两边或者起来。"

### 任务

读入一个年份，判断它是不是闰年。

### 本关新知识：把两条规则拼成一个长条件

闰年的规则有两句话，中间是"**或者**"：

1. 能被 4 整除（\`year % 4 == 0\`）**并且**不能被 100 整除（\`year % 100 != 0\`）；
2. 或者能被 400 整除（\`year % 400 == 0\`）。

**"并且"是 \`&&\`，"或者"是两个竖线 \`||\`，"不等于"是 \`!=\`。** 把规则念一遍——"（能被 4 整除 并且 不能被 100 整除）或者 能被 400 整除"——照着念的顺序写下来，就是这一行条件：

\`\`\`cpp
int year;
cin >> year;
if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
    cout << "是闰年" << endl;
} else {
    cout << "不是闰年" << endl;
}
\`\`\`

最外面那对小括号，是把"规则 1"整包起来交给 \`||\`。其实 \`&&\` 的优先级比 \`||\` 高，本题不加这对括号算出来也一样，但加上它能让人一眼看懂"先算里面、再算外面"，是很值得养成的习惯。

| 要记住的点 | 说明 |
| --- | --- |
| \`!=\` 是"不等于" | 它和 \`==\` 是一对；C++ 里根本没有 \`<>\` 这种写法 |
| \`&&\` 比 \`||\` 先算 | 就像乘除比加减先算一样，拿不准的时候**加括号**最保险 |
| 括号可以套括号 | 程序从最里面那一层开始算，往外一层层算 |
| \`==\` 和 \`=\` 别混 | \`year % 4 = 0\` 是给一个算式赋值，直接编译报错 |
| 三个数字亲自试 | \`2024\` 是闰年、\`1900\` 不是、\`2000\` 是，三组都对了才算真懂 |

### 输入格式

一行，一个整数 \`year\`（\`1 ≤ year ≤ 9999\`）。

### 输出格式

如果是闰年，输出 \`是闰年\`；否则输出 \`不是闰年\`。输出完要换行。

### 样例

**输入**

\`\`\`
2024
\`\`\`

**输出**

\`\`\`
是闰年
\`\`\`

### 说明

- \`2024\` 能被 4 整除、不能被 100 整除 → 是闰年。
- \`1900\` 能被 4 整除、也能被 100 整除，但不能被 400 整除 → 不是闰年。
- \`2000\` 能被 400 整除 → 是闰年。

### 小贴士

- 判断整除统一用取余：\`year % 4 == 0\` 表示"除以 4 余 0"，也就是能被 4 整除。
- \`!=\` 是"不等于"，别写成 \`=\` 或者 \`<>\`。
- 括号千万别省：\`&&\` 的优先级比 \`||\` 高，少了括号意思会变。
- 写完后拿 \`1900\` 和 \`2000\` 各跑一遍，确认两条规则都照顾到了。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| \`year % 400 = 0\` | 编译报错：\`=\` 是赋值，左边必须是能装东西的变量 | 判断相等用 \`==\` |
| 漏掉"不能被 100 整除"这一条 | \`1900\` 会被判成闰年（它其实不是） | 规则 1 必须写全：\`year % 4 == 0 && year % 100 != 0\` |
| 只写了规则 1，漏掉最后的 \`|| year % 400 == 0\` | \`2000\`、\`1600\` 这类整百年会被判成"不是闰年" | 末尾别忘了接上"或者能被 400 整除" |
| 用 \`<>\` 表示"不等于" | 编译报错，C++ 不认识这个符号 | 用 \`!=\` |
| \`if ((year % 4 == 0 && ...)\` 括号少写一半 | 报错 \`expected ')'\` | 把 \`(\` 和 \`)\` 数一遍，数量必须一样多 |`,
      starter_code: starter(
        '    int year;\n    cin >> year;\n    // TODO: 闰年 = (能被 4 整除 且 不能被 100 整除) 或 (能被 400 整除)\n    // 判断成立输出 "是闰年"，否则输出 "不是闰年"',
      ),
      solution_code: starter(
        '    int year;\n    cin >> year;\n    if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {\n        cout << "是闰年" << endl;\n    } else {\n        cout << "不是闰年" << endl;\n    }',
      ),
      test_cases: [
        { input: '2024\n', expected_output: '是闰年\n' },
        { input: '1900\n', expected_output: '不是闰年\n' },
        { input: '2000\n', expected_output: '是闰年\n' },
        { input: '2100\n', expected_output: '不是闰年\n' },
        { input: '2023\n', expected_output: '不是闰年\n' },
      ],
      hints: [
        '能被 4 整除写成 `year % 4 == 0`，不能被 100 整除写成 `year % 100 != 0`。',
        '两个条件要同时成立，中间用 `&&` 连接，并用括号包起来。',
        '最后再用 `|| year % 400 == 0` 接上第二条规则，整行条件就完整了。',
      ],
    },

    // ---------------------------------------------------------------- p5
    {
      id: 's2-p5',
      title: '三位数拆位',
      difficulty: '简单',
      knowledge_point: '整数除法与取余',
      description: `### 题目背景

在 C++ 里，两个**整数**做除法 \`/\`，结果只保留整数部分，小数部分会被直接丢掉：

\`\`\`
7 / 2 的结果是 3     // 不是 3.5
\`\`\`

这看起来有点"亏"，但配上取余运算符 \`%\`，就能把一个整数一位一位地拆出来。

以 \`472\` 为例：

| 想取出 | 写法 | 结果 |
| --- | --- | --- |
| 百位 | \`472 / 100\` | \`4\` |
| 十位 | \`472 / 10 % 10\` | \`7\` |
| 个位 | \`472 % 10\` | \`2\` |

\`472 / 10\` 先得到 \`47\`（个位被丢掉了），再 \`% 10\` 取出最后一位，就是十位 \`7\`。

### 任务

读入一个三位数，分别输出它的百位、十位和个位。

### 本关新知识：整数除法 \`/\` 与取余 \`%\` 配合拆位

两个**整数**做除法时，小数部分会被**直接砍掉**（不是四舍五入）：

\`\`\`
7 / 2     // 结果是 3，不是 3.5
472 / 10  // 结果是 47，个位被扔掉了
\`\`\`

被砍掉的那一位并没有消失，用取余 \`%\` 就能捡回来。所以这两位搭档的分工是：**\`/\` 负责"从右边砍掉几位"，\`%\` 负责"留下最后几位"**。以 \`472\` 为例，拆位就是把这两招反复用：

\`\`\`cpp
int n;
cin >> n;
cout << "百位：" << n / 100 << endl;       // 472 / 100 = 4，后两位被砍掉
cout << "十位：" << n / 10 % 10 << endl;   // 472 / 10 = 47，再 % 10 = 7
cout << "个位：" << n % 10 << endl;        // 直接留最后一位 = 2
\`\`\`

\`n / 10 % 10\` 里 \`/\` 和 \`%\` 是同级运算，按**从左往右**的顺序算，所以它等于 \`(n / 10) % 10\`：先把个位砍掉，再取剩下的最后一位。

| 要记住的点 | 说明 |
| --- | --- |
| 整数 ÷ 整数 = 整数 | \`472 / 10\` 得 \`47\`，小数部分是砍掉而不是四舍五入 |
| \`% 10\` 取最后一位 | 不管数字有多长，\`n % 10\` 永远是它的个位 |
| \`/ 100\` 砍掉后两位 | 三位数的百位就是 \`n / 100\` |
| \`/\` 和 \`%\` 从左往右算 | 所以 \`n / 10 % 10\` 就等于 \`(n / 10) % 10\`，不用自己加括号 |
| 用整数别用小数 | \`n / 10.0\` 会算出 \`47.2\` 这种小数，后面的 \`% 10\` 就用不了了 |
| 中文全角冒号 \`：\` | 输出文字里的冒号是中文的，直接照抄题目最稳 |

### 输入格式

一行，一个整数 \`n\`（\`100 ≤ n ≤ 999\`）。

### 输出格式

输出三行，依次是百位、十位、个位，格式如下：

\`\`\`
百位：4
十位：7
个位：2
\`\`\`

### 样例

**输入**

\`\`\`
472
\`\`\`

**输出**

\`\`\`
百位：4
十位：7
个位：2
\`\`\`

### 说明

- 某一位是 0 也要正常输出，比如输入 \`505\`，十位要输出 \`0\`。

### 小贴士

- 百位：\`n / 100\`。
- 十位：\`n / 10 % 10\`，先扔掉个位，再取最后一位。
- 个位：\`n % 10\`，直接取除以 10 的余数。
- 冒号是中文的**全角冒号** \`：\`，从题目里复制最稳妥。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| 十位写成 \`n % 100\`、百位写成 \`n / 1000\` | \`472\` 会输出 \`72\` 和 \`0\`，答案全错 | 百位 \`n / 100\`，十位 \`n / 10 % 10\` |
| \`n / 10.0 % 10\` | 除法结果变成小数，\`%\` 不能用在 \`double\` 上，编译报错 | 用整数写法 \`n / 10 % 10\` |
| 三行输出的顺序写反 | 把百位写成了个位，和期望输出对不上 | 顺序是百位 → 十位 → 个位 |
| 冒号打成英文 \`:\` | 输出 \`百位:4\`，和期望的 \`百位：4\` 不一致 | 用中文全角冒号 \`：\` |
| 某一位是 0 就跳过不输出 | 例如 \`505\` 少输出一行，格式完全错 | 是 0 也要照常输出，三行一行都不能少 |`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 分别算出百位、十位、个位\n    // TODO: 按 "百位：x" / "十位：x" / "个位：x" 的格式输出三行',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    cout << "百位：" << n / 100 << endl;\n    cout << "十位：" << n / 10 % 10 << endl;\n    cout << "个位：" << n % 10 << endl;',
      ),
      test_cases: [
        {
          input: '472\n',
          expected_output: '百位：4\n十位：7\n个位：2\n',
        },
        {
          input: '100\n',
          expected_output: '百位：1\n十位：0\n个位：0\n',
        },
        {
          input: '505\n',
          expected_output: '百位：5\n十位：0\n个位：5\n',
        },
        {
          input: '120\n',
          expected_output: '百位：1\n十位：2\n个位：0\n',
        },
        {
          input: '999\n',
          expected_output: '百位：9\n十位：9\n个位：9\n',
        },
      ],
      hints: [
        '百位用 `n / 100` 就能得到，因为整数除法会自动丢掉小数部分。',
        '十位可以先把个位扔掉：`n / 10`，再用 `% 10` 取出剩下的最后一位。',
        '个位最简单：`n % 10`。输出时别把中文全角冒号 `：` 写成英文的 `:`。',
      ],
    },

    // ---------------------------------------------------------------- p6
    {
      id: 's2-p6',
      title: '简易计算器',
      difficulty: '中等',
      knowledge_point: 'switch分支',
      description: `### 题目背景

如果程序要判断的不是"大小关系"，而是一堆并列的选项（比如用户输入的运算符是 \`+\` 还是 \`-\`），用 \`switch\` 会比一长串 \`if / else if\` 清爽得多：

\`\`\`
switch (表达式) {
    case 值1:
        // 表达式等于 值1 时执行
        break;
    case 值2:
        // 表达式等于 值2 时执行
        break;
    default:
        // 都不是时执行
        break;
}
\`\`\`

\`switch\` 括号里放一个变量，程序会跳转到值相等的那个 \`case\`。每个 \`case\` 末尾都要写 \`break;\`，否则程序会继续"滑"进下一个 \`case\` 里执行。

存一个字符要用 \`char\` 类型，字符常量写在**单引号**里，比如 \`'+'\`、\`'-'\`：

\`\`\`
int a, b;
char op;
cin >> a >> op >> b;   // 输入 3 + 5 时，a=3, op='+', b=5
\`\`\`

### 任务

读入一个形如 \`3 + 5\` 的算式，输出计算结果。支持的运算符有 \`+\`、\`-\`、\`*\`、\`/\` 四种。

### 本关新知识：\`switch / case / break\` 与 \`char\` 类型

**第一个问题：运算符怎么读进来？** 单个字符（\`+\`、\`-\` 这种）要用 \`char\` 类型来存，字符常量写在**单引号**里。一条 \`cin\` 就能把三部分一起读完，中间的空格会被自动跳过：

\`\`\`cpp
int a, b;
char op;                  // char 表示"一个字符"
cin >> a >> op >> b;      // 输入 3 + 5 时：a=3，op='+'，b=5
\`\`\`

**第二个问题：四种情况怎么分？** 要判断的不是大小关系，而是一堆并列的选项，这时 \`switch\` 比一长串 \`if / else if\` 清爽得多。它拿着 \`op\` 的值，去下面找一个相等的 \`case\`，然后从那里开始往下执行：

\`\`\`cpp
switch (op) {                     // ① 括号里放要比较的那个表达式
    case '+':                     // ② 值后面跟的是冒号，不是分号
        cout << a + b << endl;    // ③ 相等时执行这里的语句
        break;                    // ④ 干完就跳出 switch，非常重要
    case '-':
        cout << a - b << endl;
        break;
    default:                      // ⑤ 所有 case 都没匹配上时走这里
        break;
}
\`\`\`

**\`break;\` 是 \`switch\` 里最容易忘的东西。** 它相当于"到此为止，出去"。一旦漏掉，程序不会停下，而会**继续往下滑进下一个 \`case\`**，把下面的语句也执行一遍：比如 \`3 + 5\` 时 \`case '+'\` 忘了 \`break;\`，输出完 \`8\` 还会接着输出 \`-2\`。

除法的那个 \`case\` 里还要再套一层 \`if\`，因为除数不能是 0：

\`\`\`cpp
    case '/':
        if (b == 0) {                      // 先判断，再做除法
            cout << "错误：除数不能为0" << endl;
        } else {
            cout << a / b << endl;         // 整数除法：7 / 2 得 3
        }
        break;                             // 这一层的 break 也别忘
\`\`\`

| 要记住的点 | 说明 |
| --- | --- |
| \`case\` 的值用单引号 | \`case '+':\` 是字符；写成双引号就变成字符串，和 \`char\` 类型对不上 |
| \`case\` 后面是冒号 | 写成分号会直接报错 |
| 每个分支末尾都要 \`break;\` | 漏掉会滑进下一个 \`case\`，多输出一堆结果 |
| \`default:\` 兜底 | 所有 \`case\` 都没匹配上时执行，可以留空，也可以输出错误提示 |
| \`case\` 里还能再套 \`if\` | 语句可以嵌套，除数为 0 的判断就靠它 |
| \`switch\` 的 \`{ }\` 也要配对 | 里面还有 \`case\` 的冒号和 \`break;\`，最容易写漏 |

### 输入格式

一行，包含「整数 空格 运算符 空格 整数」三部分。

- 两个整数的范围是 \`-100000 ≤ 数字 ≤ 100000\`；
- 运算符一定是 \`+\`、\`-\`、\`*\`、\`/\` 中的一个。

### 输出格式

输出计算结果，输出完要换行。

- \`+\`、\`-\`、\`*\` 按普通的整数运算来算；
- \`/\` 是**整数除法**，只保留整数部分：\`7 / 2\` 输出 \`3\`；
- 做 \`/\` 时如果除数是 \`0\`，不能硬算，改输出 \`错误：除数不能为0\`。

### 样例

**输入**

\`\`\`
3 + 5
\`\`\`

**输出**

\`\`\`
8
\`\`\`

### 说明

- \`10 - 25\` 的结果是 \`-15\`，减法算出负数也要照实输出。
- 判断除数是不是 0，必须在做除法**之前**判断，否则程序会直接崩掉。

### 小贴士

- \`cin >> a >> op >> b;\` 一条语句就能读完三部分，空格会自动被跳过。
- 每个 \`case\` 后面都有冒号，末尾都有 \`break;\`——这两个地方最容易漏。
- \`case '+':\` 里面也可以再嵌套 \`if\`，两种语句可以配合使用。
- 如果还想更严谨，可以加一个 \`default:\` 分支，用来处理不认识的运算符。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| \`case "+":\`，或者 \`case '+'\` 漏了冒号 | 前者类型对不上直接报错，后者报错 \`expected ':'\` | \`case '+':\` |
| 某个 \`case\` 忘了 \`break;\` | 程序会滑进下一个 \`case\`，例如 \`3 + 5\` 先输出 \`8\` 又输出 \`-2\` | 每个分支末尾都写 \`break;\` |
| 除法不判断就直接算 | 输入 \`8 / 0\` 时整数除以 0，程序直接崩掉 | 先 \`if (b == 0)\` 输出错误提示，再用 \`else\` 做除法 |
| \`cin >> a >> op >> b;\` 写成 \`cin >> a, op, b;\` | 逗号不是输入运算符，编译报错或读不进数据 | 用 \`>>\` 一个一个连起来 |
| \`switch (op) {\` 少写右大括号 | 报错 \`expected '}'\`，行号还指向文件末尾 | 数一数 \`{\` 和 \`}\` 是否一样多 |`,
      starter_code: starter(
        "    int a, b;\n    char op;\n    cin >> a >> op >> b;\n    // TODO: 用 switch (op) 分别处理 '+', '-', '*', '/' 四种情况\n    // 注意：除法要先判断 b 是不是 0，是 0 就输出 \"错误：除数不能为0\"",
      ),
      solution_code: starter(
        '    int a, b;\n    char op;\n    cin >> a >> op >> b;\n    switch (op) {\n        case \'+\':\n            cout << a + b << endl;\n            break;\n        case \'-\':\n            cout << a - b << endl;\n            break;\n        case \'*\':\n            cout << a * b << endl;\n            break;\n        case \'/\':\n            if (b == 0) {\n                cout << "错误：除数不能为0" << endl;\n            } else {\n                cout << a / b << endl;\n            }\n            break;\n        default:\n            cout << "错误：不支持的运算符" << endl;\n            break;\n    }',
      ),
      test_cases: [
        { input: '3 + 5\n', expected_output: '8\n' },
        { input: '10 - 25\n', expected_output: '-15\n' },
        { input: '6 * 7\n', expected_output: '42\n' },
        { input: '7 / 2\n', expected_output: '3\n' },
        { input: '8 / 0\n', expected_output: '错误：除数不能为0\n' },
      ],
      hints: [
        '先写 `switch (op) {`，再依次写 `case \'+\':`、`case \'-\':`、`case \'*\':`、`case \'/\':`。',
        '每个 `case` 里输出对应算式的结果，最后一定要补上 `break;`，否则会连着执行下一个 `case`。',
        '除法的那个 `case` 里面再套一层 `if (b == 0)`：成立就输出错误提示，不成立才输出 `a / b`。',
      ],
    },
  ],
};

fs.mkdirSync(outDir, { recursive: true });
const target = path.join(outDir, 'stage-2.json');
fs.writeFileSync(target, `${JSON.stringify(stage, null, 2)}\n`, 'utf8');
console.log('written:', target, `${stage.problems.length} problems`);
