/**
 * 生成 src/data/problems/stage-3.json
 * （用脚本生成 JSON 而不是手写，避免 Markdown 里的换行/引号转义出错）
 *
 * 阶段三主题：循环结构（for / while / do-while、break、循环嵌套、累加累乘、数位拆解）
 * 严格不超纲：不使用数组、自定义函数、结构体、vector/map/sort、字符串下标与 length()
 */
import fs from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

const outDir = path.join(path.dirname(fileURLToPath(import.meta.url)), '..', '..', 'src', 'data', 'problems');

const starter = (body) => `#include <iostream>
using namespace std;

int main() {
${body}
    return 0;
}
`;

/** 生成 n 行的直角三角形（第 i 行 i 个星号） */
const triangle = (n) => {
  let out = '';
  for (let i = 1; i <= n; i++) out += `${'*'.repeat(i)}\n`;
  return out;
};

/** 生成乘法表前 n 行：第 i 行为 i*1=i*1 空 i*2=i*2 ... i*i=i*i */
const mulTable = (n) => {
  let out = '';
  for (let i = 1; i <= n; i++) {
    const row = [];
    for (let j = 1; j <= i; j++) row.push(`${i}*${j}=${i * j}`);
    out += `${row.join(' ')}\n`;
  }
  return out;
};

// 自检：乘法表前 3 行必须与手里的样例逐字一致
const SAMPLE_TABLE = '1*1=1\n2*1=2 2*2=4\n3*1=3 3*2=6 3*3=9\n';
if (mulTable(3) !== SAMPLE_TABLE) {
  throw new Error(`乘法表生成器与样例不一致：\n${JSON.stringify(mulTable(3))}`);
}
if (triangle(3) !== '*\n**\n***\n') {
  throw new Error('三角形生成器与样例不一致');
}

const stage = {
  stage: 3,
  title: '阶段三 · 让程序学会重复',
  subtitle: '循环结构',
  summary:
    '前两个阶段的程序都是从上往下一口气执行完的。这个阶段我们让程序学会**重复**：用 `for` 循环按次数重复，用 `while` 循环按条件重复，用 `break` 提前跳出。学会累加、累乘、循环嵌套画图和逐位拆解整数以后，你就能写出真正"会干活"的程序了。',
  problems: [
    // ------------------------------------------------------------------
    {
      id: 's3-p1',
      title: '从 1 加到 N',
      difficulty: '入门',
      knowledge_point: 'for 循环、累加',
      description: `### 题目背景

如果让你算 1 + 2 + 3 + …… + 100，一个一个加当然能算出来，但太累了。

计算机最擅长做的，就是这种**重复很多次、又很有规律**的事情。让程序重复执行一段代码的工具叫做**循环**，最常用的就是 \`for\` 循环：

\`\`\`cpp
for (int i = 1; i <= n; i++) {
    // 大括号里写要重复做的事情
}
\`\`\`

读法：让变量 \`i\` 从 1 开始，每次加 1（\`i++\` 就是 \`i = i + 1\`），只要 \`i\` 还小于等于 \`n\`，就把大括号里的代码再执行一遍。

### 任务

读入一个整数 \`N\`，计算 1 + 2 + 3 + … + N 的结果并输出。

### 本关新知识：\`for\` 循环与累加器

**\`for\` 循环**就是让电脑替你"把一件事重复做很多遍"的工具。它的完整长相是：

\`\`\`cpp
for (int i = 1; i <= n; i++) {
    // 这里写要重复做的事情，i 每一轮的值都不一样
}
\`\`\`

三个部分用**分号**隔开，各有各的分工：

| 位置 | 写法 | 它是干什么的 | 什么时候执行 |
| --- | --- | --- | --- |
| 第 1 部分 | \`int i = 1\` | 造一个"计数器" \`i\`，让它从 1 开始数 | 循环开始前，**只做一次** |
| 第 2 部分 | \`i <= n\` | 继续循环的**条件**，成立才再转一圈 | **每一轮开始前**都判断一次 |
| 第 3 部分 | \`i++\` | 一圈走完后计数器加 1（\`i++\` 就等于 \`i = i + 1\`） | **每一轮结束时**做一次 |

**执行顺序**（最容易搞混，按这 5 步记）：

1. 执行 \`int i = 1\`，只做这一次；
2. 判断 \`i <= n\`：不成立，整个循环立刻结束，跳到 \`}\` 后面继续往下走；
3. 成立，就执行大括号里的循环体；
4. 执行 \`i++\`，计数器加 1；
5. 回到第 2 步继续判断——这样一直重复到条件不成立为止。

用一句话概括就是：**判断 → 执行 → 加 1 → 再判断**。循环体转了几圈，第 3 部分就执行了几次。

**累加**就是准备一个"存钱罐"，每一轮往里面扔一个数，最后看总数：

\`\`\`cpp
int sum = 0;                 // ① 准备存钱罐，初值必须是 0
for (int i = 1; i <= n; i++) {
    sum += i;                // ② 把这一轮的 i 扔进去（等于 sum = sum + i;）
}
cout << sum << endl;         // ③ 等循环全部结束才输出，只输出一次
\`\`\`

**循环变量从 0 还是从 1 开始？** 看你要数的东西有几件。本题要算 1 + 2 + … + N，正好是 N 个数，所以让 \`i\` 从 1 开始、到 N 结束最自然。写成 \`i = 0\` 会白白多加一个 0，这题的结果碰巧还对，但换个题目就是错的。

| 要记住的点 | 说明 |
| --- | --- |
| 三部分之间是**分号** | \`for (int i = 1; i <= n; i++)\` 里有两个分号，第 3 部分后面**没有**分号 |
| 上界用 \`<=\` | 写 \`i < n\` 只会加到 N-1。输入 10 会得到 45，而正确答案是 55 |
| 累加器初值写 0 | 只写 \`int sum;\` 不给初值，里面是随机的垃圾数，结果完全不可控 |
| 输出语句放在循环**外面** | 放进循环体会每加一次打印一行，输出一大片数字 |
| \`i\` 的作用范围 | \`int i\` 写在 \`for\` 的括号里，出了循环就不能再用它了 |
| 大括号 \`{}\` | 循环体只有一句时可以不写大括号，但**永远写上**是个好习惯，以后加代码不容易出错 |

### 输入格式

一行，一个整数 \`N\`（\`0 ≤ N ≤ 10000\`）。

### 输出格式

输出一个整数，表示 1 到 N 的和。当 \`N = 0\` 时，和为 \`0\`。

### 样例

**输入**

\`\`\`
10
\`\`\`

**输出**

\`\`\`
55
\`\`\`

### 说明

- 1 + 2 + … + 10 = 55。
- 1 + 2 + … + 100 = 5050，可以用这个数据检查自己的程序对不对。

### 小贴士

- 累加之前要先准备一个"存钱罐"：\`int sum = 0;\`，初值必须是 0，否则结果会多出来一块。
- 循环里用 \`sum += i;\` 把每个 \`i\` 都放进去（它和 \`sum = sum + i;\` 完全一样）。
- 输出语句要写在循环**外面**，否则每加一次就会打印一行。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| \`for (int i = 1; i < n; i++)\` | 只加到 N-1，输入 10 得到 45 而不是 55 | 条件写 \`i <= n\` |
| 忘了写 \`i++\`（或误写成 \`i--\`） | 循环永远停不下来，程序运行超过 3 秒会被强制终止并提示超时 | 第 3 部分写 \`i++\` |
| 只写 \`int sum;\` 不给初值 | \`sum\` 里是随机的垃圾数，累加结果莫名其妙 | 写成 \`int sum = 0;\` |
| 把 \`cout << sum << endl;\` 写进循环体 | 每加一次就打印一行，输出一大片数字 | 输出语句放到循环的 \`}\` 外面 |
| 语句末尾漏分号 | 编译报错 \`expected ';'\` | 每条语句结尾都要有英文分号 \`;\` |
`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 定义一个变量 sum 当累加器，初值写成 0\n    // TODO: 写 for 循环，让 i 从 1 一直走到 n（包含 n）\n    // TODO: 每轮循环把 i 加到 sum 上\n    // TODO: 循环结束后输出 sum',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    int sum = 0;\n    for (int i = 1; i <= n; i++) {\n        sum += i;\n    }\n    cout << sum << endl;',
      ),
      test_cases: [
        { input: '10\n', expected_output: '55\n' },
        { input: '1\n', expected_output: '1\n' },
        { input: '0\n', expected_output: '0\n' },
        { input: '100\n', expected_output: '5050\n' },
        { input: '10000\n', expected_output: '50005000\n' },
      ],
      hints: [
        '先写 `int sum = 0;`，它就是装结果的"存钱罐"。',
        '循环写成 `for (int i = 1; i <= n; i++)`，循环体里写 `sum += i;`。',
        '`N = 0` 时循环一次都不执行，`sum` 保持 0，正好输出 0。',
      ],
    },
    // ------------------------------------------------------------------
    {
      id: 's3-p2',
      title: 'N 的阶乘',
      difficulty: '简单',
      knowledge_point: '累乘、long long',
      description: `### 题目背景

\`1 × 2 × 3 × … × N\` 叫做 **N 的阶乘**，数学上记作 \`N!\`。比如：

\`5! = 1 × 2 × 3 × 4 × 5 = 120\`

要算阶乘，只要把上一题的"累加"换成"累乘"就行。不过这里藏着一个坑：\`int\` 能存的最大整数只有 2147483647，而 \`13! = 6227020800\` 已经超过它了，程序会算出一个莫名其妙的负数。

解决办法是换一个更大的整数类型 \`long long\`，它能存到大约 9.2 × 10¹⁸，算 20 以内的阶乘绰绰有余。

### 任务

读入一个整数 \`N\`，输出 \`N!\` 的值。

### 本关新知识：累乘与 \`long long\`

这道题的循环骨架和上一题**一模一样**，变的只有两件事：存钱罐的初值，和每一轮往里面放东西的动作。

**累乘**就是一个"乘起来的存钱罐"：

\`\`\`cpp
long long result = 1;            // ① 累乘器：初值必须是 1，不能是 0
for (int i = 1; i <= n; i++) {
    result *= i;                 // ② 把这一轮的 i 乘进去（等于 result = result * i;）
}
cout << result << endl;          // ③ 循环结束后输出
\`\`\`

**为什么累加写 0、累乘要写 1？** 因为初值必须是"不改变结果"的那个数：0 加上任何数还是那个数，所以累加从 0 开始；而 0 乘任何数永远是 0，累乘就必须从 1 开始。这就是"中性值"的意思——像往空碗里加米，碗本身不能自带米，也不能是个无底洞。

| 对比 | 累加（上一题） | 累乘（本题） |
| --- | --- | --- |
| 存钱罐初值 | \`int sum = 0;\` | \`long long result = 1;\` |
| 每轮的动作 | \`sum += i;\` | \`result *= i;\` |
| 初值写错的后果 | 结果多一块或变随机数 | 不管乘多少，结果永远是 0 |

**\`long long\` 是什么？** 它是"更能装"的整数盒子。\`int\` 最大只能存 2147483647，而 \`13! = 6227020800\` 已经装不下了——装不下的后果不是报错，而是数字悄悄"溢出"成一个莫名其妙的负数。\`long long\` 能存到大约 9.2 × 10¹⁸，本题到 20 都用不完。

\`\`\`cpp
int a = 13;                  // 只是举例
long long b = 6227020800;    // 超出 int 范围的数，必须用 long long 装
cout << b << endl;           // 输出 long long 和输出 int 一样，直接用 cout 就行
\`\`\`

| 要记住的点 | 说明 |
| --- | --- |
| 累乘初值 \`1\` | 写成 0 的话，0 乘任何数都是 0，输出恒为 0 |
| 变量类型用 \`long long\` | 用 \`int\` 时从 13! 开始出错，会输出负数或别的怪数字 |
| 输入 0 要输出 1 | 数学规定 \`0! = 1\`。循环 \`for (i = 1; i <= 0; ...)\` 一次都不执行，\`result\` 保持 1，正好符合要求 |
| 循环变量 \`i\` 从 1 开始 | 从 0 开始的话会多乘一个 0，结果直接变成 0 |
| \`*=\` 的含义 | \`result *= i;\` 就是 \`result = result * i;\`，写哪个都对 |
| 输出在循环外 | 写在循环体里会输出 N 行中间结果 |

### 输入格式

一行，一个整数 \`N\`（\`0 ≤ N ≤ 20\`）。

### 输出格式

输出一个整数，表示 \`N!\`。

### 样例

**输入**

\`\`\`
5
\`\`\`

**输出**

\`\`\`
120
\`\`\`

### 说明

- 数学上规定 \`0! = 1\`（一个数都不乘，结果是 1），所以输入 \`0\` 时要输出 \`1\`。
- 输入 \`20\` 时输出 \`2432902008176640000\`，这已经接近 \`long long\` 的上限，所以题目最多只问到 20。
- **累乘的初值必须是 1**。如果习惯性地写成 0，那不管乘多少都还是 0。

### 小贴士

- 定义变量时写成 \`long long result = 1;\`。
- 循环体里写 \`result *= i;\`（等价于 \`result = result * i;\`）。
- 把 \`result\` 定义成 \`int\` 的话，输入 13 就会得到错误答案，可以自己试试看。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| \`long long result = 0;\` | 0 乘任何数还是 0，不管输入几都输出 0 | 初值写成 1 |
| \`int result = 1;\` | 输入 13 开始溢出，输出一个负数或怪数字 | 换成 \`long long result = 1;\` |
| 循环体写成 \`result += i;\` | 算成了 1 + 2 + … + N（累加），5 会输出 15 而不是 120 | 用 \`result *= i;\` |
| 循环写成 \`for (int i = 0; i <= n; i++)\` | 多乘了一个 0，结果永远是 0 | \`i\` 从 1 开始，条件写 \`i <= n\` |
| 把输出写在循环体里面 | 每乘一次打印一行，输出一大堆中间结果 | 输出放到循环的 \`}\` 外面 |
`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 定义一个 long long 变量 result 当累乘器，初值写成 1（不能写 0）\n    // TODO: 写 for 循环，让 i 从 1 一直走到 n\n    // TODO: 每轮循环把 result 乘上 i\n    // TODO: 循环结束后输出 result',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    long long result = 1;\n    for (int i = 1; i <= n; i++) {\n        result *= i;\n    }\n    cout << result << endl;',
      ),
      test_cases: [
        { input: '5\n', expected_output: '120\n' },
        { input: '0\n', expected_output: '1\n' },
        { input: '1\n', expected_output: '1\n' },
        { input: '13\n', expected_output: '6227020800\n' },
        { input: '20\n', expected_output: '2432902008176640000\n' },
      ],
      hints: [
        '累乘器和累加器不一样：`long long result = 1;` 初值是 1。',
        '循环体里写 `result *= i;`。',
        '输入 13 的结果 6227020800 已经超出 `int` 范围，所以变量一定要用 `long long`。',
      ],
    },
    // ------------------------------------------------------------------
    {
      id: 's3-p3',
      title: '打印直角三角形',
      difficulty: '简单',
      knowledge_point: '嵌套循环、打印图形',
      description: `### 题目背景

打印图形是练习**循环嵌套**（一个循环里面再写一个循环）的经典题目。

嵌套循环的分工很明确：

- **外层循环**决定"现在是第几行"；
- **内层循环**决定"这一行要打印几个符号"。

外面走一步，里面转一整圈，这样就能一行一行地把图案画出来。

### 任务

读入一个整数 \`N\`，打印一个 N 行的直角三角形：第 1 行 1 个星号，第 2 行 2 个星号……第 \`N\` 行 \`N\` 个星号，所有星号都从行首开始排（左对齐）。

### 本关新知识：嵌套循环与打印图形

**嵌套循环**就是"循环里面再套一个循环"。它的规矩只有一条，但极其重要：

> **外层循环走一步，内层循环就要从头到尾走完一整个循环。**

就像钟表：分针转一整圈（内层跑完），时针才走一格（外层走一步）。

\`\`\`cpp
for (int i = 1; i <= n; i++) {        // 外层：管"现在是第几行"
    for (int j = 1; j <= i; j++) {    // 内层：管"这一行打印几个星号"
        cout << "*";                  // 打印一个星号，注意这里不换行
    }
    cout << endl;                     // 一行打完才换行（在内层循环外面！）
}
\`\`\`

**跟着 \`n = 3\` 走一遍**：

| 外层第几轮 | 内层 \`j\` 的取值 | 这一行打印出来 |
| --- | --- | --- |
| \`i = 1\` | \`j = 1\` | \`*\` |
| \`i = 2\` | \`j = 1, 2\` | \`**\` |
| \`i = 3\` | \`j = 1, 2, 3\` | \`***\` |

注意内层循环的条件用的是 **\`i\`**，不是 \`n\`。正因如此，第 1 行内层只转 1 圈、第 2 行转 2 圈……行号越大星号越多，画出来才是三角形。如果写成 \`j <= n\`，每行都是 n 个星号，就变成一个正方形了。

**哪一层管什么？** 记住这张分工表就不会乱：

| 层 | 变量 | 负责 | 在本题里决定什么 |
| --- | --- | --- | --- |
| 外层 | \`i\` | 行 | 一共打印几行。本题第 \`i\` 行正好要 \`i\` 个星号，所以 \`i\` 一个变量两用 |
| 内层 | \`j\` | 列 | 这一行从左往右打印几个符号 |
| 换行 | \`cout << endl;\` | 收尾 | 属于外层循环，内层跑完一整圈才执行一次 |

| 要记住的点 | 说明 |
| --- | --- |
| 内外层变量名不能相同 | 两层都写 \`int i\` 会互相踩，内层习惯换个名字写 \`j\` |
| 内层条件用 \`i\` | 用 \`n\` 就打印不出三角形，只会得到矩形 |
| \`endl\` 的位置 | 放在内层循环的 \`}\` 之后、外层循环的 \`}\` 之前 |
| 星号后面不加空格 | \`cout << "*";\` 才对；写成 \`"* "\` 每行末尾会多出空格，与期望输出不一致 |
| 大括号要配对 | 两层循环一共两对 \`{}\`，少一个就编译不过 |

### 输入格式

一行，一个整数 \`N\`（\`1 ≤ N ≤ 20\`）。

### 输出格式

输出 \`N\` 行，第 \`i\` 行有 \`i\` 个星号 \`*\`。每行末尾都要换行，行首和行末都**不要**多输出空格。

### 样例

**输入**

\`\`\`
3
\`\`\`

**输出**

\`\`\`
*
**
***
\`\`\`

### 说明

- 输入 \`1\` 时只输出一行 \`*\`。
- 输入 \`20\` 时会输出 20 行，最后一行有 20 个星号。

### 小贴士

- 外层循环：\`for (int i = 1; i <= n; i++)\`。因为第 \`i\` 行正好要 \`i\` 个星号，所以 \`i\` 可以直接当成星号个数用。
- 内层循环：\`for (int j = 1; j <= i; j++) cout << "*";\`，注意条件里用的是 \`i\`，不是 \`n\`。
- 内层循环结束后别忘了 \`cout << endl;\` 换行，否则所有星号会挤在同一行。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| 内层写成 \`for (int j = 1; j <= n; j++)\` | 每行都是 N 个星号，输出一个 N×N 的矩形 | 条件写 \`j <= i\` |
| 内外层都写 \`int i\` | 内层把外层的 \`i\` 改乱，行数和星号数全错，甚至变成死循环 | 内层换名字写 \`j\` |
| 忘了 \`cout << endl;\` | 所有星号挤在同一行：\`**********\` | 每行打完都要换行 |
| 把 \`cout << endl;\` 写进内层循环 | 每个星号各占一行，输出变成竖着的一列 | 换行语句放到内层 \`}\` 外面 |
| 输出 \`cout << "* ";\` | 每行末尾多一个空格，逐字比对不通过 | 只输出 \`*\`，不加空格 |
`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 外层 for 循环让 i 从 1 走到 n，i 表示"第几行"（也是这一行的星号个数）\n    // TODO: 内层 for 循环每行输出 i 个星号\n    // TODO: 每行输出完之后用 endl 换行',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= i; j++) {\n            cout << "*";\n        }\n        cout << endl;\n    }',
      ),
      test_cases: [
        { input: '3\n', expected_output: '*\n**\n***\n' },
        { input: '1\n', expected_output: '*\n' },
        { input: '5\n', expected_output: triangle(5) },
        { input: '20\n', expected_output: triangle(20) },
      ],
      hints: [
        '两层循环要分开想：外层管行数，内层管这一行打印几个。',
        '内层循环的条件是 `j <= i`，用 `i` 才能让每一行的星号个数逐行增加。',
        '换行的 `cout << endl;` 要放在内层循环**外面**、外层循环**里面**。',
      ],
    },
    // ------------------------------------------------------------------
    {
      id: 's3-p4',
      title: '九九乘法表',
      difficulty: '中等',
      knowledge_point: '嵌套循环与乘法表',
      description: `### 题目背景

九九乘法表是循环嵌套最实用的例子：外层循环负责"第几行"，内层循环负责把这一行的每个算式都算出来打印出去。

它和上一题打印三角形长得几乎一样，区别只是把"打印星号"换成了"打印算式"。

### 任务

读入一个整数 \`N\`，输出乘法表的前 \`N\` 行。

输出格式约定（很重要，请照着样例逐字理解）：

- 第 \`i\` 行从左到右依次输出 \`j = 1, 2, …, i\` 的算式；
- 第 \`j\` 个算式写成 \`i*j=积\`，乘号两边、等号两边都**不加空格**；
- 同一行里的算式之间用**一个空格**分隔；
- **行末不要有多余空格**；
- 每行末尾换行。

### 本关新知识：嵌套循环输出算式与控制行末空格

骨架和上一题打印三角形**完全一样**，只是把"打印一个星号"换成了"打印一个算式"。分工也照旧：外层 \`i\` 管第几行，内层 \`j\` 管这一行里的第几个算式（第 \`i\` 行只输出 \`j = 1, 2, …, i\`）。

**怎么输出一个算式？** 数字直接送进 \`cout\`，乘号和等号要写成带双引号的字符串：

\`\`\`cpp
cout << i << "*" << j << "=" << i * j;    // i=3、j=2 时，屏幕上出现 3*2=6
\`\`\`

拆开读：\`i\` 是数字变量，输出它的值；\`"*"\` 是文字乘号，原样显示；\`i * j\` 会让电脑**先算乘法**、再把结果输出。

| 写法 | 屏幕上是 | 说明 |
| --- | --- | --- |
| \`cout << i * j;\` | \`6\` | 先计算，再输出计算结果 |
| \`cout << "i*j";\` | \`i*j\` | 引号里的内容原样显示，电脑不会去算它 |
| \`cout << "*";\` | \`*\` | 字符串，就是乘号本身 |
| \`cout << *;\` | 编译报错 | 不带引号的 \`*\` 是乘法运算符，不能单独输出 |

**本题最容易踩的坑是行末空格。** 如果每输出一个算式就在后面补一个空格，那么行末就会拖一个空格，而评测是**逐字符比对**的，末尾多一个空格直接不通过。小技巧是把空格加在算式的**前面**，而且第一个算式不加：

\`\`\`cpp
for (int j = 1; j <= i; j++) {
    if (j > 1) {
        cout << " ";              // 只有第二个及以后的算式，前面才补一个空格
    }
    cout << i << "*" << j << "=" << i * j;    // 然后输出算式本身
}
cout << endl;                     // 这一行全部输出完才换行
\`\`\`

这样一来，空格永远夹在两个算式中间：第一个算式前面没有空格，最后一个算式后面也没有空格，行末干干净净。

| 要记住的点 | 说明 |
| --- | --- |
| 内层上界是 \`i\` | 第 \`i\` 行只有 \`i\` 个算式，不是 \`n\` 个 |
| 空格加在算式**前**面 | 只在 \`j > 1\` 时输出一个空格，行末就不会多空格 |
| 换行的位置 | \`cout << endl;\` 放在内层循环外面、外层循环里面 |
| 乘号是字符串 \`"*"\` | 带引号才是文字乘号，不带引号是乘法运算 |
| 内层变量不能叫 \`i\` | 和外层重名会互相干扰，习惯写 \`j\` |
| 乘积不加引号 | \`i * j\` 会算出结果；\`"i*j"\` 只会原样显示三个字符 |

### 输入格式

一行，一个整数 \`N\`（\`1 ≤ N ≤ 9\`）。

### 输出格式

输出 \`N\` 行，每行末尾换行。

### 样例

**输入**

\`\`\`
3
\`\`\`

**输出**

\`\`\`
1*1=1
2*1=2 2*2=4
3*1=3 3*2=6 3*3=9
\`\`\`

### 说明

- 本题**不需要**把每一列对齐，也不用补空格，照着样例原样输出即可。
- 输入 \`9\` 时会输出完整的九九乘法表，共 9 行，第 9 行有 9 个算式。

### 小贴士

- 外层循环 \`i\` 从 1 到 \`n\`，内层循环 \`j\` 从 1 到 \`i\`。
- 输出一个算式：\`cout << i << "*" << j << "=" << i * j;\`
- 控制行末空格的小技巧：只在 \`j > 1\` 时才先输出一个空格（\`if (j > 1) cout << " ";\`），这样空格永远出现在两个算式中间。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| 每个算式后面都补一个空格 | 每行末尾多一个空格，逐字符比对不通过 | 把空格加在算式前面：\`if (j > 1) cout << " ";\` |
| 乘号不带引号写成 \`cout << i * j;\` | 中间少了一个乘号，输出变成 \`3 2=6\` 这种样子 | 乘号是字符串：\`cout << "*"\` |
| 内层写成 \`for (int j = 1; j <= n; j++)\` | 每行都输出 n 个算式，第 1 行变成 \`1*1=1 1*2=2 1*3=3\` | 内层上界写 \`i\` |
| 乘积写成 \`"i*j"\`（加了引号） | 屏幕上原样显示 \`i*j\`，不是算出来的数 | 乘积不加引号：\`i * j\` |
| 换行语句写进了内层循环 | 每个算式各占一行，输出变成 9 行一列 | 换行放到内层 \`}\` 外面 |
`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 外层 for 循环让 i 从 1 走到 n，表示第 i 行\n    // TODO: 内层 for 循环让 j 从 1 走到 i，按 i*j=积 的格式输出每个算式\n    // TODO: 算式之间输出一个空格，注意行末不要多输出空格\n    // TODO: 每行结束后换行',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= i; j++) {\n            if (j > 1) {\n                cout << " ";\n            }\n            cout << i << "*" << j << "=" << i * j;\n        }\n        cout << endl;\n    }',
      ),
      test_cases: [
        { input: '3\n', expected_output: mulTable(3) },
        { input: '1\n', expected_output: mulTable(1) },
        { input: '5\n', expected_output: mulTable(5) },
        { input: '9\n', expected_output: mulTable(9) },
      ],
      hints: [
        '骨架和上一题一样：外层 `i` 从 1 到 `n`，内层 `j` 从 1 到 `i`。',
        '一个算式这样输出：`cout << i << "*" << j << "=" << i * j;`。',
        '空格不能无脑加在算式后面（那样行末会多一个空格），可以只在 `j > 1` 时先输出一个空格。',
      ],
    },
    // ------------------------------------------------------------------
    {
      id: 's3-p5',
      title: '数位之和',
      difficulty: '简单',
      knowledge_point: 'while 循环、逐位拆解',
      description: `### 题目背景

整数在程序里可以像积木一样拆开：

- 取出一个整数 \`n\` 的**最后一位**：用 \`n % 10\`（对 10 取余数）；
- 把最后一位**扔掉**：用 \`n /= 10\`（除以 10，整数除法会丢掉小数部分）。

拿 \`n = 12345\` 举例，反复做这两步：

| 第几轮 | \`n % 10\`（取出的数字） | \`n /= 10\` 之后的 n |
| --- | --- | --- |
| 1 | 5 | 1234 |
| 2 | 4 | 123 |
| 3 | 3 | 12 |
| 4 | 2 | 1 |
| 5 | 1 | 0 |

\`n\` 变成 0 就没得拆了，循环自然结束。

注意到这里"要重复几轮"事先是不知道的（得看 \`n\` 有几位），这种"条件成立就一直做"的场合，用 \`while\` 循环最合适：

\`\`\`cpp
while (n > 0) {
    // 只要 n 还是正数，就一直重复
}
\`\`\`

### 任务

读入一个非负整数 \`n\`，求它各个数位上的数字之和。

### 本关新知识：\`while\` 循环与逐位拆解整数

**\`while\` 循环**的长相很简单：

\`\`\`cpp
while (条件) {
    // 只要条件成立，就一直重复这里
}
\`\`\`

它和 \`for\` 最大的区别是：**先判断，后执行**。先看条件，成立才进去做；条件一开始就不成立，循环体**一次都不执行**。\`for\` 适合"次数事先就知道"的场合（比如 1 加到 N），\`while\` 适合"不知道要转几圈、只知道什么情况下该继续"的场合。本题要拆几位数，得看 \`n\` 有几位，事先并不知道，所以用 \`while\` 最合适。

**两个拆数字的工具**（像剪纸：一刀剪下最后一位，一步把最后一位丢掉）：

| 写法 | 意思 | 例子（n = 12345） |
| --- | --- | --- |
| \`n % 10\` | 取**最后一位**。\`%\` 是取余数，而除以 10 的余数正好就是末位数字 | 得到 5 |
| \`n /= 10\` | **扔掉最后一位**。整数除法会自动丢掉小数部分 | 变成 1234 |

**"处理整数的每一位"就这么一个固定套路**，以后遇到数位之和、数位个数、数字倒序都是同一套：

\`\`\`cpp
int sum = 0;              // ① 累加器：装数位之和，初值 0
while (n > 0) {           // ② 先判断：n 还有位没拆完吗？
    sum += n % 10;        // ③ 取出最后一位，加进 sum
    n /= 10;              // ④ 把最后一位扔掉；少了这一步 n 永远不变，就死循环了
}
cout << sum << endl;      // ⑤ 循环结束后输出
\`\`\`

**跟着 \`n = 12345\` 走一遍**：

| 第几轮 | 判断 \`n > 0\` | \`sum += n % 10\` | \`n /= 10\` 之后 | \`sum\` 的值 |
| --- | --- | --- | --- | --- |
| 1 | 12345 > 0 成立 | 加 5 | 1234 | 5 |
| 2 | 1234 > 0 成立 | 加 4 | 123 | 9 |
| 3 | 123 > 0 成立 | 加 3 | 12 | 12 |
| 4 | 12 > 0 成立 | 加 2 | 1 | 14 |
| 5 | 1 > 0 成立 | 加 1 | 0 | 15 |
| 6 | 0 > 0 不成立 | 不做了 | 不做了 | 循环结束，输出 15 |

| 要记住的点 | 说明 |
| --- | --- |
| 先判断后执行 | 条件不成立时循环体一次也不做——这正是输入 \`0\` 能正确输出 \`0\` 的原因，不需要额外特判 |
| 顺序是"先取位、后缩位" | 反过来先缩位的话，最后一位就被丢掉了，永远加不进 \`sum\` |
| \`%\` 和 \`/\` 别写反 | 取末位用 \`% 10\`，扔掉末位用 \`/ 10\` |
| 循环里必须让 \`n\` 变小 | 每轮都要写 \`n /= 10;\`，否则条件永远成立，程序运行超过 3 秒会被强制终止并提示超时 |
| 累加器初值 0 | \`int sum = 0;\` |
| 输出放在循环外 | 写在循环里会输出每一步的中间结果，而不是最终的数位和 |

### 输入格式

一行，一个整数 \`n\`（\`0 ≤ n ≤ 1000000000\`）。

### 输出格式

输出一个整数，表示 \`n\` 的数位之和。

### 样例

**输入**

\`\`\`
12345
\`\`\`

**输出**

\`\`\`
15
\`\`\`

### 说明

- 1 + 2 + 3 + 4 + 5 = 15。
- 输入 \`0\` 时输出 \`0\`：\`0\` 的数位和就是它自己。
- 输入 \`1000000000\` 时输出 \`1\`；输入 \`999999999\` 时输出 \`81\`。

### 小贴士

- 先写累加器：\`int sum = 0;\`。
- 循环里两件事的顺序是"先加、后缩"：先 \`sum += n % 10;\`，再 \`n /= 10;\`。顺序反了的话，最后一位就被丢掉了。
- 循环写 \`while (n > 0)\` 时，输入 0 会一次都不执行、直接输出 0，正好符合要求，不需要额外特判。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| 循环体里先写 \`n /= 10;\` 再写 \`sum += n % 10;\` | 最后一位被跳过，12345 会输出 14 而不是 15 | 先取位 \`sum += n % 10;\`，再缩位 \`n /= 10;\` |
| 忘了写 \`n /= 10;\` | \`n\` 永远不变、条件恒成立 → 死循环，运行超过 3 秒会被强制终止并提示超时 | 每一轮都要缩位 |
| 条件写成 \`while (n >= 0)\` | \`n\` 缩到 0 之后仍然满足 \`>= 0\`，变成死循环 | 写 \`while (n > 0)\` |
| 取位写成 \`sum += n / 10;\` | 加进去的是"去掉末位之后的数"，结果完全不对 | 取末位用 \`% 10\` |
| 只写 \`int sum;\` 不给初值 | \`sum\` 是随机垃圾值，输出莫名其妙 | \`int sum = 0;\` |
`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 定义一个变量 sum 当数位之和的累加器，初值写成 0\n    // TODO: 用 while (n > 0) 循环：每轮先用 n % 10 取出最后一位加到 sum 上\n    // TODO: 再用 n /= 10 去掉最后一位，直到 n 变成 0\n    // TODO: 循环结束后输出 sum',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    int sum = 0;\n    while (n > 0) {\n        sum += n % 10;\n        n /= 10;\n    }\n    cout << sum << endl;',
      ),
      test_cases: [
        { input: '12345\n', expected_output: '15\n' },
        { input: '0\n', expected_output: '0\n' },
        { input: '9\n', expected_output: '9\n' },
        { input: '1000000000\n', expected_output: '1\n' },
        { input: '999999999\n', expected_output: '81\n' },
      ],
      hints: [
        '取最后一位用 `n % 10`，去掉最后一位用 `n /= 10`。',
        '循环体里必须**先**累加、**后**缩小，顺序反了会少算一位。',
        '输入 0 时 `while (n > 0)` 一次都不执行，直接输出 0，正好是对的。',
      ],
    },
    // ------------------------------------------------------------------
    {
      id: 's3-p6',
      title: '判断素数',
      difficulty: '中等',
      knowledge_point: '循环加 break、标志变量',
      description: `### 题目背景

**素数**（也叫质数）是指**大于 1**、并且只能被 1 和它自己整除的整数。比如 2、3、5、7、11 都是素数，而 4、6、9 不是。

要判断一个数 \`n\` 是不是素数，最直接的办法就是：从 2 开始一个个试着除。

- 只要发现有一个数能整除 \`n\`，就说明 \`n\` 不是素数，这时没必要再试下去，可以立刻跳出循环——这个"提前结束循环"的动作叫 \`break\`。
- 如果试到最后都没找到能整除 \`n\` 的数，那它就是素数。

其实也不用一直试到 \`n - 1\`：如果 \`n\` 有比 1 大的因数，那么它一定有一个因数不超过 \`√n\`。所以只要试到 \`i * i <= n\` 就足够了，速度会快很多。

### 任务

读入一个整数 \`n\`，判断它是不是素数。

### 本关新知识：标志变量、\`break\` 与试除范围

先把思路说成人话：拿 2、3、4……一个个去试着除 \`n\`，只要找到**任意一个**能整除它的数，就说明它不是素数。像在一排抽屉里找钥匙，只要找到了，后面的抽屉就不用再翻了。

**① 标志变量**——就是门口挂的一块牌子，先挂上"是素数"，一旦发现反例就翻个面：

\`\`\`cpp
bool isPrime = true;    // 先假设它是素数。bool 类型只能装 true / false 两个值
\`\`\`

最后只要看牌子决定输出哪句话：\`if (isPrime) cout << "是素数"; else cout << "不是素数";\`

**② \`break\` 是"提前结束循环"**：程序执行到 \`break;\` 时，立刻跳出**当前这一层**循环，跑到循环的 \`}\` 后面接着往下走。本题只有一层循环，跳出来正好就到了输出的那一步。

**③ 试除到哪为止？** 不用试到 \`n - 1\`。道理是：如果 \`n = a × b\` 而且 \`a ≤ b\`，那么小的那个因数 \`a\` 一定不超过 \`√n\`。所以在 2 到 \`√n\` 之间找不到因数，更大的那一半也肯定找不到。代码里写成 \`i * i <= n\`（比调用 \`sqrt\` 方便，也不用担心小数误差）。

把三件事拼在一起，就是本题的核心代码：

\`\`\`cpp
bool isPrime = true;                    // ① 先假设它是素数
if (n < 2) {
    isPrime = false;                    // ② 1 不是素数，单独处理
} else {
    for (int i = 2; i * i <= n; i++) {  // ③ 只试到 √n 就够了
        if (n % i == 0) {               // ④ 发现能整除 → 说明还有别的因数
            isPrime = false;            // ⑤ 翻牌子
            break;                      // ⑥ 后面的数不用再试，直接跳出
        }
    }
}
\`\`\`

**跟着两个例子走一遍**：

- \`n = 15\`：\`i = 2\` 时 \`15 % 2 != 0\`；\`i = 3\` 时 \`15 % 3 == 0\` → 翻牌子 + \`break\`，输出"不是素数"。
- \`n = 17\`：\`i\` 试到 4（\`4 * 4 = 16 <= 17\` 成立）都不整除；\`i\` 变成 5 时 \`5 * 5 = 25 > 17\`，循环自然结束，牌子还是 \`true\`，输出"是素数"。

| 要记住的点 | 说明 |
| --- | --- |
| 标志初值写 \`true\` | 先假设"是素数"，然后去找反例；写反了结论就全反 |
| 试除从 2 开始 | 从 1 开始的话 \`n % 1 == 0\` 永远成立，所有数都会被判成不是素数 |
| 条件写 \`i * i <= n\` | 等价于 \`i <= √n\`。写成 \`i * i < n\` 时，4、9 这类平方数会被误判成素数 |
| \`n < 2\` 必须特判 | 1 不是素数，可它的循环一次都不执行、牌子仍是 \`true\`，不特判就会错判 |
| \`break\` 该写在哪 | 放在"发现能整除"的那个 \`if\` 里面，只在这一种情况下才跳出循环 |
| \`bool\` 怎么输出 | 不要 \`cout << isPrime;\`（只会打印 1 或 0），要用 \`if / else\` 输出中文 |

### 输入格式

一行，一个整数 \`n\`（\`1 ≤ n ≤ 1000000\`）。

### 输出格式

如果 \`n\` 是素数，输出一行 \`是素数\`；否则输出一行 \`不是素数\`。

### 样例

**输入**

\`\`\`
17
\`\`\`

**输出**

\`\`\`
是素数
\`\`\`

### 说明

- \`1\` **不是**素数（规定素数必须大于 1），所以输入 \`1\` 要输出 \`不是素数\`。
- \`2\` 是最小的素数，也是唯一的偶数素数。
- 题目保证 \`n ≤ 1000000\`，按上面的方法程序很快就能算完。

### 小贴士

- 用一个**标志变量**记录判断结果：\`bool isPrime = true;\`，一开始先假设"它是素数"。
- \`n < 2\`（也就是 \`1\`）要单独处理成"不是素数"，否则会被误判。
- 发现能整除的数时：把标志改成 \`false\`，然后 \`break;\` 跳出循环。
- 循环结束后用 \`if (isPrime)\` 决定输出哪一句。

### 常见错误

| 你可能写成 | 会发生什么 | 正确写法 |
| --- | --- | --- |
| 试除写成 \`for (int i = 1; i * i <= n; i++)\` | \`n % 1 == 0\` 永远成立，所有数都被判成"不是素数" | \`i\` 从 2 开始 |
| 忘了处理 \`n < 2\` | 输入 1 时循环一次都不执行，牌子还是 \`true\`，会输出"是素数" | 先写 \`if (n < 2) isPrime = false;\` |
| 条件写成 \`i * i < n\` | 4、9、25 这些平方数被误判成素数 | 用 \`i * i <= n\` |
| 标志变量初值写成 \`bool isPrime = false;\` | 牌子一开始就是"不是素数"，任何输入都输出"不是素数" | 初值写 \`true\` |
| 整除判断写成 \`n / i == 0\` | 意思变成"n 比 i 小"，判断逻辑完全错 | 判断整除用取余：\`n % i == 0\` |
`,
      starter_code: starter(
        '    int n;\n    cin >> n;\n    // TODO: 定义一个 bool 标志变量 isPrime，一开始先假设它是素数\n    // TODO: 单独处理 n < 2 的情况（1 不是素数）\n    // TODO: 用 for 循环检查 2 到 sqrt(n)（也就是 i * i <= n）之间有没有能整除 n 的数\n    // TODO: 一旦发现（n % i == 0），把标志改成 false 并 break 跳出循环\n    // TODO: 根据 isPrime 输出"是素数"或"不是素数"',
      ),
      solution_code: starter(
        '    int n;\n    cin >> n;\n    bool isPrime = true;\n    if (n < 2) {\n        isPrime = false;\n    } else {\n        for (int i = 2; i * i <= n; i++) {\n            if (n % i == 0) {\n                isPrime = false;\n                break;\n            }\n        }\n    }\n    if (isPrime) {\n        cout << "是素数" << endl;\n    } else {\n        cout << "不是素数" << endl;\n    }',
      ),
      test_cases: [
        { input: '17\n', expected_output: '是素数\n' },
        { input: '1\n', expected_output: '不是素数\n' },
        { input: '2\n', expected_output: '是素数\n' },
        { input: '4\n', expected_output: '不是素数\n' },
        { input: '999983\n', expected_output: '是素数\n' },
      ],
      hints: [
        '标志变量写法：`bool isPrime = true;`，只要找到反例就改成 `false`。',
        '循环可以写成 `for (int i = 2; i * i <= n; i++)`，一旦 `n % i == 0` 就 `break;`。',
        '别忘了 `1` 不是素数，需要单独判断 `n < 2`。',
      ],
    },
  ],
};

fs.mkdirSync(outDir, { recursive: true });
const target = path.join(outDir, 'stage-3.json');
fs.writeFileSync(target, `${JSON.stringify(stage, null, 2)}\n`, 'utf8');
console.log('written:', target, `${stage.problems.length} problems`);
