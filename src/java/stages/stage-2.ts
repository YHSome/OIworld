/**
 * 阶段二 · 运算符与判断：取余、逻辑运算、if 分支与 switch（6 道题）
 *
 * 阶段一解决了"把数据读进来、算一算、打出去"，这一阶段让程序学会"看情况做决定"：
 * 先用 `%` 取余数，再用 `&& || !=` 把条件拼起来，最后用 if / else if / else 阶梯和 switch 走进不同分支。
 */

import type { StageData } from '../../types/problem';
import { lesson, mainOnly, mistakesTable, pointsTable } from '../lesson.ts';

export const STAGE_2: StageData = {
  stage: 2,
  title: '阶段二 · 运算符与判断',
  subtitle: '运算符、if / else 与 switch',
  summary:
    '程序只会从上往下一条条执行是不够的，它还需要"看情况做决定"。\n\n' +
    '这一阶段先补齐运算符：用 `%` 求余数，用 `> < >= <= == !=` 比较大小，' +
    '用 `&& || !` 把几个条件拼成一个；然后让程序走进不同的分支——两个出口用 `if / else`，' +
    '多个出口用 `if / else if / else` 阶梯，一堆并列的选项用 `switch / case / break`。\n\n' +
    '**写条件时记住两件事**：条件要放在圆括号 `( )` 里，判断相等要用两个等号 `==`；' +
    '一个等号 `=` 是"赋值"，写错会出大问题。',
  problems: [
    lesson({
      id: 'j2-1',
      title: '奇数还是偶数',
      difficulty: '入门',
      knowledge: '取余与 if / else',
      story:
        '生活中我们常常要给数字分类：门牌号是单号还是双号、座位是奇数排还是偶数排……\n\n' +
        '数学里，**能被 2 整除**的整数叫**偶数**，不能被 2 整除的叫**奇数**。' +
        '前面学过 `+ - * /`，这一题要用一个新运算符 `%`（读作"取余"）来回答"除以 2 剩不剩东西"。\n\n' +
        '光会算还不够，程序还要能**看情况做决定**——这就是 `if / else`（读作"如果……否则……"）。',
      task: '读入一个整数 `n`，判断它是奇数还是偶数：偶数输出 `偶数`，奇数输出 `奇数`。',
      lesson: `**第一步：认识取余运算符 \`%\`**

\`%\` 算的是"除完以后剩下多少"。可以把它想成**分糖果**：7 颗糖 2 个人平分，每人拿 3 颗，最后剩下 1 颗，这个"剩下的 1 颗"就是 \`7 % 2\` 的结果。

\`\`\`java
System.out.println(7 % 2);   // 输出 1：7 除以 2，商 3 余 1
System.out.println(8 % 2);   // 输出 0：正好分完，一颗都不剩
\`\`\`

**偶数的标志就是"除以 2 正好分完"**，也就是余数为 0，写成条件就是 \`n % 2 == 0\`。

**第二步：用 \`if / else\` 让程序分岔**

\`if\` 读作"如果"，\`else\` 读作"否则"。这是让程序"看情况做决定"的最基本句型：

\`\`\`java
if (条件) {
    // 条件成立时，执行这里的语句
} else {
    // 条件不成立时，执行这里的语句
}
\`\`\`

**圆括号** \`( )\` 里放条件（一个能回答"是 / 不是"的问题），**大括号** \`{ }\` 里放要执行的语句。把上面的条件填进去，本题就写完了：

\`\`\`java
if (n % 2 == 0) {                  // 条件：n 除以 2 的余数等于 0 吗？
    System.out.println("偶数");     // 成立 —— 它正好能被 2 整除
} else {
    System.out.println("奇数");     // 不成立 —— 剩下的一律算奇数
}
\`\`\`

${pointsTable([
  ['`%` 是取余运算', '`n % 2` 得到的是余数。它长得像百分号，但在这里是一个运算符'],
  ['`==` 才是"等于"', '一个 `=` 是赋值（把右边的值装进左边的变量），两个 `==` 才是判断相等'],
  ['条件必须放在 `( )` 里', '写成 `if n % 2 == 0` 少了圆括号，编译直接报错'],
  ['条件后面不写分号', '写成 `if (...);` 会让 `if` 变成一句空话，后面的 `else` 也找不到配对的 `if`'],
  ['`{ }` 要成对出现', '每个 `{` 都要有对应的 `}`；少写一个，报错行号常常指到很后面，很难找'],
])}

**注意文字要用英文双引号包起来**：\`System.out.println("偶数");\` 里的引号是英文的 \`"\`，中文引号 \`“ ”\` 编译器不认识。`,
      inputFormat: '一行，一个整数 `n`（`-1000 ≤ n ≤ 1000`）。',
      outputFormat:
        '如果 `n` 是偶数就输出 `偶数`，否则输出 `奇数`；无论哪一种，末尾都要换行。',
      sample:
        '### 样例\n\n**输入**\n\n```\n7\n```\n\n**输出**\n\n```\n奇数\n```\n\n' +
        '### 说明\n\n' +
        '- `0` 是偶数：0 除以 2 余 0，正好分完。\n' +
        '- 负数也分奇偶：`-3` 是奇数，`-8` 是偶数。\n' +
        '- 程序只判断"是不是偶数"，剩下的一律交给 `else`，所以连负数都不用特殊处理。',
      mistakes: mistakesTable([
        ['`if (n % 2 = 0)`', '编译报错：`=` 是赋值，而 `n % 2` 这样的算式不是能装东西的变量', '判断相等用两个等号：`n % 2 == 0`'],
        ['用 `n % 2 == 1` 判断奇数', '`-3 % 2` 的结果是 `-1` 而不是 `1`，负数会被判成偶数', '只判断偶数 `n % 2 == 0`，其余交给 `else`'],
        ['`if (n % 2 == 0);` 后面才写 `{ }`', '多出来的分号让 `if` 空转，`else` 找不到配对的 `if`，编译报错', '条件后面不写分号，直接接 `{`'],
        ['`System.out.println(偶数);` 忘了引号', '报错 `cannot find symbol: variable 偶数`', '要输出的文字放在英文双引号里：`"偶数"`'],
        ['用中文引号 `“偶数”`', '报错 `illegal character`，编译器只认英文半角引号', '改成英文双引号 `"偶数"`'],
      ]),
      tips:
        '- `%` 和 `/` 是一对搭档：`/` 给你商，`%` 给你余数。\n' +
        '- `-3 % 2` 在 Java 里的结果是 `-1`，但它依然不等于 0，所以会被正确判成奇数。\n' +
        '- 点「运行」之前，记得在"标准输入"框里填一个数字（例如 `7`），否则程序会一直等输入。',
      starter: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        // TODO: 用 n % 2 判断奇偶，偶数输出 "偶数"，奇数输出 "奇数"
        // 提示：if (n % 2 == 0) { ... } else { ... }，两条分支各写一条 System.out.println`,
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        if (n % 2 == 0) {
            System.out.println("偶数");
        } else {
            System.out.println("奇数");
        }`,
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '7\n', expected: '奇数\n' },
        { input: '0\n', expected: '偶数\n' },
        { input: '-3\n', expected: '奇数\n' },
        { input: '-8\n', expected: '偶数\n' },
      ],
      hints: [
        '判断条件就是 `n % 2 == 0`：除以 2 的余数是 0，它就是偶数。',
        '把判断放进 `if (...) { ... } else { ... }`，两条分支各写一条 `System.out.println`。',
        '负数不用特殊处理：`-3 % 2` 的结果是 `-1`，它不等于 0，照样会被判成奇数。',
      ],
    }),

    lesson({
      id: 'j2-2',
      title: '三个数中的最大值',
      difficulty: '简单',
      knowledge: '逻辑运算与多个分支',
      story:
        '三个朋友比身高，想知道谁最高。\n\n' +
        '上一题只回答"是 / 不是"两种可能，这一题有**三种**可能：a 最高、b 最高、c 最高。' +
        '要同时问两个问题（"a 比 b 大吗"**并且**"a 比 c 大吗"），就得学会把几个小条件拼成一个大条件。',
      task: '读入三个整数 `a`、`b`、`c`，输出其中的最大值。',
      lesson: `**第一步：把两个比较拼成一个条件**

要判断"a 是三个数里最大的"，其实要回答两个问题：**a 大于等于 b 吗**、**a 大于等于 c 吗**。两个都成立才算数，这就要用到**逻辑运算符**：

${pointsTable([
  ['`&&`（并且）', '两边**都**成立，整个条件才成立。例如 `a >= b && a >= c`，念成"a 大于等于 b，并且 a 大于等于 c"'],
  ['`||`（或者）', '两边**有一个**成立，整个条件就成立。例如 `a >= b || a >= c`'],
  ['`!`（取反）', '把成立变不成立、不成立变成立。例如 `!(a > b)` 和 `a <= b` 是一个意思'],
  ['写成 `&&` 而不是 `&`', '`&&` 是"并且"：两边**都**成立才为真，而且左边不成立时右边不再计算；一个 `&` 是另一套运算，别用它来拼条件'],
])}

**第二步：多个出口，用 \`else if\` 排成一条链**

三种可能，\`if / else\` 的两个出口装不下，那就接着写 \`else if\`：

\`\`\`java
if (a >= b && a >= c) {          // ① a 是最大的吗？（两个问题都要成立）
    System.out.println(a);
} else if (b >= c) {             // ② 不是 a，那答案只能在 b、c 里，比一比就好
    System.out.println(b);
} else {                         // ③ 前面都不成立，只剩下 c 了
    System.out.println(c);
}
\`\`\`

程序**从上往下**一条条检查，命中一条就结束，后面的条件不再看。所以第二层只要问 \`b >= c\`：能走到这一行，说明 a 已经不是最大的了。

${pointsTable([
  ['用 `>=` 而不是 `>`', '题目允许"并列最大"：输入 `5 5 3` 时最大值还是 `5`。`>=` 的意思是"不小于"，比 `>` 更贴合"谁最大"，也更保险'],
  ['`else if` 要连在一起写', '中间不能插分号；另外 `else` 后面不能带条件，带条件的只能写 `else if`'],
  ['最后一个 `else` 兜底', '剩下的情况不用再写条件，直接写 `else` 就行'],
  ['嵌套 `if` 也能做对', '在 `if` 的 `{ }` 里再写一层 `if` 同样能做对这题，但用 `&&` 拼条件往往更短更好读'],
])}`,
      inputFormat: '一行，三个整数 `a`、`b`、`c`（`-1000000 ≤ a, b, c ≤ 1000000`），用空格分隔。',
      outputFormat: '输出一个整数，表示三个数里最大的那个，末尾换行。',
      sample:
        '### 样例\n\n**输入**\n\n```\n3 7 5\n```\n\n**输出**\n\n```\n7\n```\n\n' +
        '### 说明\n\n' +
        '- 最大值可能出现不止一次：输入 `5 5 3`，答案还是 `5`，不用说明它出现了几次。\n' +
        '- 负数也要能比：输入 `-1 -5 -3`，最大值是 `-1`（最接近 0 的那个）。',
      mistakes: mistakesTable([
        ['`if (a > b & a > c)`', '一个 `&` 不是"并且"的标准写法（它两边都会算一遍），容易看错，也养不成好习惯', '逻辑"并且"要写两个符号 `&&`'],
        ['第一层写成 `if (a <= b && a <= c)`（方向比反了）', '把最小值当成了最大值：输入 `3 7 5` 会输出 `3`', '要最大值就用"不小于"：`a >= b && a >= c`'],
        ['`else (b >= c) { ... }`', '编译报错：`else` 后面不能跟条件', '带条件的分支要写 `else if (b >= c)`'],
        ['三个分支写成三个独立的 `if`', '数字相等时会输出好几行：输入 `10 10 10` 会打印三次 `10`', '后面的分支改成 `else if` / `else`，命中一条就结束'],
        ['条件里写成 `if (a = b)`', '编译报错：`a = b` 是赋值，得到的是数字，而条件需要的是"是 / 否"', '判断相等要用 `==`'],
      ]),
      tips:
        '- "a 最大"的条件是 `a >= b && a >= c`，两个比较缺一不可。\n' +
        '- 第一层 `if` 不成立时，a 已经出局，答案只能是 b 或 c，所以第二层只要比一次。\n' +
        '- 三个数相等的情况别忘了：输入 `5 5 5` 时也要输出 `5`，用 `>=` 就能兜住。',
      starter: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        int b = scanner.nextInt();
        int c = scanner.nextInt();
        // TODO: 判断三个数里谁最大，并输出它
        // 提示："a 最大"的条件是 a >= b && a >= c，不成立时再用 else if 比 b 和 c`,
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        int b = scanner.nextInt();
        int c = scanner.nextInt();
        if (a >= b && a >= c) {
            System.out.println(a);
        } else if (b >= c) {
            System.out.println(b);
        } else {
            System.out.println(c);
        }`,
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '3 7 5\n', expected: '7\n' },
        { input: '10 10 10\n', expected: '10\n' },
        { input: '-1 -5 -3\n', expected: '-1\n' },
        { input: '-1000000 0 1000000\n', expected: '1000000\n' },
      ],
      hints: [
        '"a 最大"的条件是 `a >= b && a >= c`，两个比较都要成立。',
        '第一层 `if` 不成立时，a 已经出局，答案只能是 b 或 c，这时用 `else if (b >= c)` 就够了。',
        '剩下那种情况直接写 `else`，不用再写条件。',
        '比较时用 `>=` 而不是 `>`：题目允许有并列的最大值（比如 `5 5 3` 的答案还是 `5`）。',
      ],
    }),

    lesson({
      id: 'j2-3',
      title: '成绩等级',
      difficulty: '简单',
      knowledge: 'if / else if / else 阶梯',
      story:
        '老师要根据分数给等级。规则有好几条，程序需要从上往下一一检查，命中哪条就走哪条——' +
        '这就是 `if / else if / else` 阶梯。\n\n' +
        '最重要的一点：程序是**从上往下**检查的，只要有一条命中，后面的条件就**不再检查**了。' +
        '所以区间要从"最高的"排到"最低的"。',
      task:
        '读入一个分数 `score`，按下面的规则输出等级：\n\n' +
        '| 分数区间 | 等级 |\n' +
        '| --- | --- |\n' +
        '| 90 分及以上 | 优秀 |\n' +
        '| 80 ~ 89 分 | 良好 |\n' +
        '| 70 ~ 79 分 | 中等 |\n' +
        '| 60 ~ 69 分 | 及格 |\n' +
        '| 60 分以下 | 不及格 |',
      lesson: `**一条阶梯，代替一大堆 \`if\`**

分数有五个区间，"成立 / 不成立"两个出口不够用了，这时候把多个 \`if\` 串成一条**阶梯**：

\`\`\`java
if (条件1) {
    // 条件1 成立 → 走这里
} else if (条件2) {
    // 条件1 不成立、但条件2 成立 → 走这里
} else {
    // 上面的都不成立 → 走这里
}
\`\`\`

阶梯的精髓是"**从上往下、命中即停**"：程序先问条件1，成立就执行它的大括号，然后**整条阶梯直接结束**，后面的条件看都不看。

这就带来一个大方便：**从第二层开始不用再写上限**。能走到 \`else if (score >= 80)\` 这一行，说明第一层的 \`score >= 90\` 已经不成立了，分数必然小于 90。所以只要从高分往低分一路排下去：

\`\`\`java
if (score >= 90) {                        // 先问最高那一档
    System.out.println("优秀");
} else if (score >= 80) {                 // 能走到这里，说明分数已经小于 90
    System.out.println("良好");
} else if (score >= 70) {                 // 继续往下退一档
    System.out.println("中等");
}
// 70 分以下的两档，照这个套路补完即可
\`\`\`

${pointsTable([
  ['顺序必须从高到低', '把 `score >= 60` 写在最前面，95 分也会先命中"及格"，等级就全错了'],
  ['中间层用 `else if` 连写', '写成一个个独立的 `if`，各层互不排斥，一个分数可能输出好几行等级'],
  ['边界用 `>=`', '`score > 90` 会让 90 分掉进"良好"，而题目要求 90 分算优秀'],
  ['最后用 `else` 收尾', '60 分以下只剩这一种可能，不用再写条件，写 `else` 就行'],
  ['不能连写比较', '`90 >= score >= 80` 这种写法在 Java 里不合法：程序会先算 `90 >= score` 得到一个"是 / 否"，再拿它去和 80 比较，直接编译报错'],
])}`,
      inputFormat: '一行，一个整数 `score`（`0 ≤ score ≤ 100`）。',
      outputFormat: '输出等级文字：`优秀`、`良好`、`中等`、`及格` 或 `不及格`，末尾换行。',
      sample:
        '### 样例\n\n**输入**\n\n```\n95\n```\n\n**输出**\n\n```\n优秀\n```\n\n' +
        '### 说明\n\n' +
        '- 边界要看仔细：`90` 分算优秀，`89` 分算良好；`60` 分算及格，`59` 分算不及格。\n' +
        '- 每一档只输出一行等级，不会输出两行。',
      mistakes: mistakesTable([
        ['把 `if (score >= 60)` 放在第 1 层', '95 分先命中"及格"，等级全错', '区间从高到低排，最高档放最前面'],
        ['中间层写成一个个独立的 `if`', '各层互不排斥，一个分数可能输出好几行等级', '中间层统一用 `else if` 连起来'],
        ['`else if (90 >= score >= 80)` 连写比较', '编译报错：`90 >= score` 得到的是"是 / 否"，不能拿去和数字 80 比大小', '拆成两层：`score >= 90`、`score >= 80`'],
        ['`if (score > 90)`', '90 分被算成"良好"，和题目要求的边界不一致', '用 `score >= 90`'],
        ['只写到 `else if (score >= 60)` 就结束了', '59 分不满足任何条件，一行都不输出', '末尾补上 `else { System.out.println("不及格"); }`'],
      ]),
      tips:
        '- 第一条写 `if (score >= 90)`。它不成立时，你其实已经知道分数小于 90 了，后面不必再写上限。\n' +
        '- 接着写 `else if (score >= 80)`、`else if (score >= 70)`……最后用 `else` 收尾。\n' +
        '- 顺序绝对不能乱：如果把 `score >= 60` 写在最前面，90 分也会被判成"及格"。',
      starter: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int score = scanner.nextInt();
        // TODO: 用 if / else if / else 判断分数落在哪个区间
        // 90 分及以上 → "优秀"，往下依次是 "良好"、"中等"、"及格"，其余 "不及格"`,
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int score = scanner.nextInt();
        if (score >= 90) {
            System.out.println("优秀");
        } else if (score >= 80) {
            System.out.println("良好");
        } else if (score >= 70) {
            System.out.println("中等");
        } else if (score >= 60) {
            System.out.println("及格");
        } else {
            System.out.println("不及格");
        }`,
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '95\n', expected: '优秀\n' },
        { input: '89\n', expected: '良好\n' },
        { input: '60\n', expected: '及格\n' },
        { input: '59\n', expected: '不及格\n' },
      ],
      hints: [
        '第一条写 `if (score >= 90)`，注意是"大于等于"，90 分也算优秀。',
        '中间的每一层都用 `else if`：能走到下一层，就说明前面的条件已经不成立了。',
        '最后一层用 `else` 兜底，60 分以下的分数都会走到那里。',
        '不要写成 `90 >= score >= 80` 这种连写比较，Java 不支持，会直接编译报错。',
      ],
    }),

    lesson({
      id: 'j2-4',
      title: '闰年判断',
      difficulty: '简单',
      knowledge: '&& 与 || 组合条件',
      story:
        '地球绕太阳转一圈大约要 365.2422 天，多出来的零头靠"闰年"补上：闰年的二月有 29 天，全年 366 天。\n\n' +
        '判断闰年的规则有两句话：能被 4 整除**并且**不能被 100 整除，**或者**能被 400 整除。' +
        '这一题的重点，就是把这两句话翻译成程序里的一行条件。',
      task: '读入一个年份 `year`，判断它是不是闰年。',
      lesson: `**把两句规则翻译成一行条件**

闰年的规则有两句话，中间是"**或者**"：

1. 能被 4 整除（\`year % 4 == 0\`）**并且**不能被 100 整除（\`year % 100 != 0\`）；
2. 或者能被 400 整除（\`year % 400 == 0\`）。

**"并且"是 \`&&\`，"或者"是两条竖线 \`||\`，"不等于"是 \`!=\`。** 照着自己念的顺序写下来，就是这一行条件：

\`\`\`java
if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
    System.out.println("是闰年");
} else {
    System.out.println("不是闰年");
}
\`\`\`

最外面那对小括号，是把"规则 1"整包起来交给 \`||\`，让人一眼看懂"先算里面、再算外面"。完整程序里，前面还要把年份读进来：

\`\`\`java
Scanner scanner = new Scanner(System.in);
int year = scanner.nextInt();
\`\`\`

**为什么规则要写得这么绕？看三个年份就懂了：**

| 年份 | 能被 4 整除？ | 能被 100 整除？ | 能被 400 整除？ | 是闰年吗 |
| --- | --- | --- | --- | --- |
| 2024 | 是 | 否 | 否 | **是**：满足规则 1 |
| 1900 | 是 | 是 | 否 | **不是**：两条规则都不满足 |
| 2000 | 是 | 是 | 是 | **是**：满足规则 2 |

${pointsTable([
  ['`!=` 是"不等于"', '它和 `==` 是一对；Java 里没有 `<>` 这种写法，写成 `<>` 会直接报错'],
  ['`&&` 比 `||` 先算', '和"乘除比加减先算"一个道理。拿不准的时候，加括号最保险'],
  ['括号可以套括号', '程序从最里面那一层开始算，往外一层层算；写完数一数 `(` 和 `)` 是否一样多'],
  ['`==` 和 `=` 别混', '`year % 400 = 0` 是给一个算式赋值，左边装不了东西，编译直接报错'],
  ['三个年份亲自试', '`2024` 是闰年、`1900` 不是、`2000` 是，三组都对了才算真懂'],
])}`,
      inputFormat: '一行，一个整数 `year`（`1 ≤ year ≤ 9999`）。',
      outputFormat: '如果是闰年输出 `是闰年`，否则输出 `不是闰年`，末尾换行。',
      sample:
        '### 样例\n\n**输入**\n\n```\n2024\n```\n\n**输出**\n\n```\n是闰年\n```\n\n' +
        '### 说明\n\n' +
        '- `2024` 能被 4 整除、不能被 100 整除 → 是闰年。\n' +
        '- `1900` 能被 4 整除、也能被 100 整除，但不能被 400 整除 → 不是闰年。\n' +
        '- `2000` 能被 400 整除 → 是闰年（整百年里的特例）。',
      mistakes: mistakesTable([
        ['`year % 400 = 0`', '编译报错：`=` 是赋值，左边必须是能装东西的变量', '判断相等用 `==`'],
        ['漏掉"不能被 100 整除"这一条', '`1900` 会被判成闰年（它其实不是）', '规则 1 要写全：`year % 4 == 0 && year % 100 != 0`'],
        ['漏掉最后的 `|| year % 400 == 0`', '`2000`、`1600` 这类整百年会被判成"不是闰年"', '末尾接上"或者能被 400 整除"'],
        ['用 `<>` 表示"不等于"', '编译报错，Java 不认这个符号', '用 `!=`'],
        ['括号少写一个 `)`', '报错提示缺少右括号，行号常常指到很后面', '数一遍 `(` 和 `)` 的个数是否一样'],
      ]),
      tips:
        '- 判断整除统一用取余：`year % 4 == 0` 表示"除以 4 余 0"，也就是能被 4 整除。\n' +
        '- 写完后拿 `1900` 和 `2000` 各跑一遍，确认两条规则都照顾到了。\n' +
        '- 两个条件同时成立用 `&&`，两种可能满足一个就行用 `||`，别把这两个弄反。',
      starter: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int year = scanner.nextInt();
        // TODO: 闰年 = (能被 4 整除 且 不能被 100 整除) 或 (能被 400 整除)
        // 成立输出 "是闰年"，否则输出 "不是闰年"`,
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int year = scanner.nextInt();
        if ((year % 4 == 0 && year % 100 != 0) || year % 400 == 0) {
            System.out.println("是闰年");
        } else {
            System.out.println("不是闰年");
        }`,
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '2024\n', expected: '是闰年\n' },
        { input: '1900\n', expected: '不是闰年\n' },
        { input: '2000\n', expected: '是闰年\n' },
        { input: '2023\n', expected: '不是闰年\n' },
      ],
      hints: [
        '能被 4 整除写成 `year % 4 == 0`，不能被 100 整除写成 `year % 100 != 0`。',
        '这两个条件要同时成立，中间用 `&&` 连起来，再用一对圆括号包住。',
        '最后接上 `|| year % 400 == 0`（或者能被 400 整除），整行条件才完整。',
        '写完后拿 `1900` 和 `2000` 各跑一遍，确认两条规则都照顾到了。',
      ],
    }),

    lesson({
      id: 'j2-5',
      title: '三位数拆位',
      difficulty: '简单',
      knowledge: '整数除法与取余',
      story:
        '在 Java 里，两个**整数**做除法 `/`，结果只保留整数部分，小数部分会被直接丢掉：' +
        '`7 / 2` 的结果是 `3`，不是 3.5。\n\n' +
        '这看起来有点"亏"，但配上取余运算符 `%`，就能把一个整数一位一位地拆出来。',
      task: '读入一个三位数 `n`，分别输出它的百位、十位和个位，每个占一行。',
      lesson: `**整数除法会把小数部分砍掉**

两个**整数**做除法时，小数部分会被**直接砍掉**（不是四舍五入）：

\`\`\`java
System.out.println(7 / 2);      // 输出 3，不是 3.5
System.out.println(472 / 10);   // 输出 47，个位被扔掉了
\`\`\`

被砍掉的那一位并没有消失，用取余 \`%\` 就能把它捡回来。所以这两位搭档的分工是：**\`/\` 负责"从右边砍掉几位"，\`%\` 负责"留下最后几位"**。以 \`472\` 为例：

| 想取出 | 写法 | 结果 |
| --- | --- | --- |
| 百位 | \`472 / 100\` | \`4\` |
| 十位 | \`472 / 10 % 10\` | \`7\` |
| 个位 | \`472 % 10\` | \`2\` |

\`472 / 10\` 先得到 \`47\`（个位被扔掉了），再 \`% 10\` 取出最后一位，就是十位 \`7\`。写成代码就是：

\`\`\`java
System.out.println("百位：" + n / 100);       // 472 / 100 = 4，后两位被砍掉
System.out.println("十位：" + n / 10 % 10);   // 472 / 10 = 47，再 % 10 = 7
System.out.println("个位：" + n % 10);        // 直接留最后一位 = 2
\`\`\`

\`n / 10 % 10\` 里 \`/\` 和 \`%\` 是同级运算，按**从左往右**的顺序算，所以它等于 \`(n / 10) % 10\`：先把个位砍掉，再取剩下的最后一位。

${pointsTable([
  ['整数 ÷ 整数 = 整数', '`472 / 10` 得 `47`，小数部分是"砍掉"而不是四舍五入'],
  ['`% 10` 取最后一位', '不管数字有多长，`n % 10` 永远是它的个位'],
  ['`/ 100` 砍掉后两位', '三位数的百位就是 `n / 100`'],
  ['`/` 和 `%` 从左往右算', '所以 `n / 10 % 10` 就等于 `(n / 10) % 10`，不用自己加括号'],
  ['用整数别用小数', '`n / 10.0` 算出的是小数：`472 / 10.0 % 10` 得 `7.2`，输出 `十位：7.2` 就和期望的 `十位：7` 对不上了'],
])}`,
      inputFormat: '一行，一个整数 `n`（`100 ≤ n ≤ 999`）。',
      outputFormat:
        '输出三行，依次是百位、十位、个位，格式如下：\n\n' +
        '```\n百位：4\n十位：7\n个位：2\n```\n\n' +
        '冒号是**中文全角冒号** `：`，不要打成英文的 `:`。',
      sample:
        '### 样例\n\n**输入**\n\n```\n472\n```\n\n**输出**\n\n```\n百位：4\n十位：7\n个位：2\n```\n\n' +
        '### 说明\n\n' +
        '- 某一位是 0 也要正常输出，比如输入 `505`，十位要输出 `0`，三行一行都不能少。\n' +
        '- 冒号是**中文全角冒号** `：`（占一个汉字宽），不是英文的 `:`。',
      mistakes: mistakesTable([
        ['十位写成 `n % 100`、百位写成 `n / 1000`', '`472` 会输出 `72` 和 `0`，答案全错', '百位 `n / 100`，十位 `n / 10 % 10`'],
        ['`n / 10.0 % 10`', '结果变成小数：`472 / 10.0 % 10` 得 `7.2`，会输出 `十位：7.2`，与期望不一致', '用整数写法 `n / 10 % 10`'],
        ['三行输出的顺序写反', '把个位写成了百位，和期望输出对不上', '顺序是百位 → 十位 → 个位'],
        ['冒号打成英文 `:`', '输出 `百位:4`，期望的是 `百位：4`，评测不通过', '用中文全角冒号 `：`，从题面复制最稳'],
        ['某一位是 0 就跳过不输出', '比如 `505` 少输出一行，格式全错', '是 0 也要照常输出，三行一行都不能少'],
      ]),
      tips:
        '- 百位：`n / 100`；十位：`n / 10 % 10`（先扔掉个位，再取最后一位）；个位：`n % 10`。\n' +
        '- 中文标点是全角字符，建议直接从题面复制，不要用输入法打。\n' +
        '- 拿 `505` 自己试一遍：十位应该是 0，如果少输出一行就说明写错了。',
      starter: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        // TODO: 分别算出百位、十位、个位
        // TODO: 按 "百位：x" / "十位：x" / "个位：x" 的格式输出三行（冒号是中文全角）`,
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt();
        System.out.println("百位：" + n / 100);
        System.out.println("十位：" + n / 10 % 10);
        System.out.println("个位：" + n % 10);`,
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '472\n', expected: '百位：4\n十位：7\n个位：2\n' },
        { input: '100\n', expected: '百位：1\n十位：0\n个位：0\n' },
        { input: '505\n', expected: '百位：5\n十位：0\n个位：5\n' },
        { input: '999\n', expected: '百位：9\n十位：9\n个位：9\n' },
      ],
      hints: [
        '百位是 `n / 100`：整数除法会把后两位直接砍掉。',
        '十位先把个位扔掉：`n / 10`，再用 `% 10` 取出剩下的最后一位。',
        '个位最简单：`n % 10`。',
        '输出时冒号是中文全角 `：`，别打成英文的 `:`。',
      ],
    }),

    lesson({
      id: 'j2-6',
      title: '简易计算器',
      difficulty: '中等',
      knowledge: 'switch / case / break',
      story:
        '如果程序要判断的不是"大小关系"，而是一堆并列的选项——比如用户输入的运算符是 `+` 还是 `-`——' +
        '用 `switch` 会比一长串 `if / else if` 清爽得多。\n\n' +
        '这一题把前面学的运算符和判断合起来用，做一个能算加、减、乘、除的小计算器。',
      task:
        '读入一个形如 `3 + 5` 的算式，输出计算结果。支持的运算符有 `+`、`-`、`*`、`/` 四种；' +
        '做除法时如果除数是 `0`，不能硬算，改成输出 `错误：除数不能为0`。',
      lesson: `**第一步：把运算符读进来**

\`+\`、\`-\` 这种**单个字符**要用 \`char\` 类型来存，字符值写在**单引号**里：\`'+'\` 是一个字符，\`"+"\` 是一段文字（\`String\`）——两者不是一回事。

Scanner 没有"读一个字符"的方法，读单个字符的**固定写法**是先读一小段文字、再取它的第一个字符（\`charAt(0)\` 就是"取第 0 个、也就是第一个字符"的意思，这里照抄即可）：

\`\`\`java
int a = scanner.nextInt();           // 先读第一个整数
char op = scanner.next().charAt(0);  // 再读运算符（固定写法）
int b = scanner.nextInt();           // 最后读第二个整数
\`\`\`

输入 \`3 + 5\` 时，空格会被自动跳过：a 是 3、op 是 \`'+'\`、b 是 5。

**第二步：用 \`switch\` 处理并列的几种情况**

要判断的不是大小关系，而是一堆并列的选项，这时 \`switch\` 比一长串 \`if / else if\` 清爽得多。它拿着 \`op\` 的值，去下面找一个相等的 \`case\`，然后从那里开始往下执行：

\`\`\`java
switch (op) {                       // ① 括号里放要比较的那个变量
    case '+':                       // ② 值后面跟的是冒号，不是分号
        System.out.println(a + b);  // ③ 相等时执行这里的语句
        break;                      // ④ 干完就跳出 switch，非常重要
    case '-':
        System.out.println(a - b);
        break;
    default:                        // ⑤ 所有 case 都没匹配上时走这里
        System.out.println("错误：不支持的运算符");
        break;
}
\`\`\`

**\`break;\` 是 \`switch\` 里最容易忘的东西。** 它相当于"到此为止，出去"。一旦漏掉，程序不会停下，而会**继续往下滑进下一个 \`case\`**，把下面的语句也执行一遍：比如 \`3 + 5\` 时 \`case '+'\` 忘了 \`break;\`，先输出 \`8\`，还会接着输出 \`-2\`。

除法的那个 \`case\` 里还要再套一层 \`if\`，因为除数不能是 0：

\`\`\`java
    case '/':
        if (b == 0) {                          // 先判断，再做除法
            System.out.println("错误：除数不能为0");
        } else {
            System.out.println(a / b);         // 整数除法：7 / 2 得 3
        }
        break;                                 // 这一层的 break 也别忘
\`\`\`

${pointsTable([
  ['`case` 的值用单引号', '`case \'+\':` 是字符；写成双引号就变成 `String`，和 `char` 类型对不上，编译报错'],
  ['`case` 后面是冒号', '写成分号会直接报错；冒号容易漏，写完一个 `case` 先看它的末尾'],
  ['每个分支末尾都要 `break;`', '漏掉会"滑"进下一个 `case`，多输出一堆结果'],
  ['`default:` 兜底', '所有 `case` 都没匹配上时执行；本题保证运算符是四种之一，写它是个好习惯'],
  ['`case` 里还能再套 `if`', '语句可以嵌套，除数是不是 0 的判断就靠它'],
  ['`switch` 的 `{ }` 也要配对', '里面还有 `case` 的冒号和 `break;`，大括号最容易写漏'],
])}`,
      inputFormat:
        '一行，包含「整数 空格 运算符 空格 整数」三部分。\n\n' +
        '- 两个整数的范围是 `-100000 ≤ 数字 ≤ 100000`；\n' +
        '- 运算符一定是 `+`、`-`、`*`、`/` 中的一个。',
      outputFormat:
        '输出计算结果，末尾换行：\n\n' +
        '- `+`、`-`、`*` 按普通的整数运算来算；\n' +
        '- `/` 是**整数除法**，只保留整数部分：`7 / 2` 输出 `3`；\n' +
        '- 做 `/` 时如果除数是 `0`，输出 `错误：除数不能为0`。',
      sample:
        '### 样例\n\n**输入**\n\n```\n3 + 5\n```\n\n**输出**\n\n```\n8\n```\n\n' +
        '### 说明\n\n' +
        '- 输入 `10 - 25` 要输出 `-15`：减法算出负数也要原样输出。\n' +
        '- `/` 是整数除法：输入 `7 / 2` 输出 `3`，小数部分被砍掉。\n' +
        '- 输入 `8 / 0` 要输出 `错误：除数不能为0`，而不是让程序崩掉。',
      mistakes: mistakesTable([
        ["`char op = \"+\";` 用了双引号", '类型对不上：双引号是 `String`，`char` 只能装单引号里的**一个字符**', "`char op = '+';`"],
        ['某个 `case` 忘了 `break;`', '程序会继续滑进下一个 `case`：算 `3 + 5` 时先输出 `8`，又输出 `-2`', '每个分支末尾都写 `break;`'],
        ['`case "+":` 用了双引号', '`op` 是 `char`，而 `"+"` 是 `String`，编译报错 `incompatible types`', "写成 `case '+':`（单引号）"],
        ['除法不判断就直接算', '输入 `8 / 0` 时整数除以 0，程序直接崩掉（算术异常）', '先 `if (b == 0)` 输出错误提示，再 `else` 做除法'],
        ['`switch (op) {` 少写右大括号', '报错 `reached end of file while parsing`，行号指向文件末尾', '数一数 `{` 和 `}` 是否一样多'],
      ]),
      tips:
        '- 三行把三部分读进来：`int a = scanner.nextInt();`、`char op = scanner.next().charAt(0);`、`int b = scanner.nextInt();`。\n' +
        '- 每个 `case` 后面是冒号，分支末尾是 `break;`——这两处最容易漏。\n' +
        '- `case \'+\':` 里的引号是**单引号**（一个字符），写成双引号就变成文字了。\n' +
        '- 一定要**先判断除数是不是 0，再做除法**，否则程序会直接崩掉。',
      starter: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        char op = scanner.next().charAt(0);
        int b = scanner.nextInt();
        // TODO: 用 switch (op) 分别处理 '+', '-', '*', '/' 四种情况，每个分支末尾别忘了 break;
        // TODO: 除法的分支里要先判断 b 是不是 0，是 0 就输出 "错误：除数不能为0"`,
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        `        Scanner scanner = new Scanner(System.in);
        int a = scanner.nextInt();
        char op = scanner.next().charAt(0);
        int b = scanner.nextInt();
        switch (op) {
            case '+':
                System.out.println(a + b);
                break;
            case '-':
                System.out.println(a - b);
                break;
            case '*':
                System.out.println(a * b);
                break;
            case '/':
                if (b == 0) {
                    System.out.println("错误：除数不能为0");
                } else {
                    System.out.println(a / b);
                }
                break;
            default:
                System.out.println("错误：不支持的运算符");
                break;
        }`,
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '3 + 5\n', expected: '8\n' },
        { input: '10 - 25\n', expected: '-15\n' },
        { input: '7 / 2\n', expected: '3\n' },
        { input: '8 / 0\n', expected: '错误：除数不能为0\n' },
      ],
      hints: [
        "先读三部分：`int a = scanner.nextInt();`、`char op = scanner.next().charAt(0);`、`int b = scanner.nextInt();`。",
        "写 `switch (op) {`，再依次写 `case '+':`、`case '-':`、`case '*':`、`case '/':` 四个分支。",
        '每个 `case` 里输出对应算式的结果，末尾一定要补上 `break;`，否则会连着执行下一个 `case`。',
        '除法的分支里再套一层 `if (b == 0)`：成立就输出 `错误：除数不能为0`，不成立才输出 `a / b`。',
      ],
    }),
  ],
};
