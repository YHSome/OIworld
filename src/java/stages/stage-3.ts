/**
 * 阶段三 · 循环：让程序学会重复（6 道题）
 *
 * 前两个阶段的程序都是从第一行一路执行到最后一行，跑完就结束。
 * 这一阶段让程序学会**重复**：用 `for` 按次数重复，用 `while` 按条件重复，用 `break` 提前跳出。
 * 顺序沿用 C++ 靶场阶段三：累加 → 累乘 → 嵌套循环画图 → 乘法表 → 拆解整数 → 判断素数。
 *
 * 所有代码都必须是 Java 8 语法（浏览器里跑的是 Doppio JVM），
 * 并且要足够轻量：循环次数控制在千次量级，输出不超过几十行。
 */

import type { StageData } from '../../types/problem';
import { lesson, mainOnly, mistakesTable, pointsTable } from '../lesson.ts';

export const STAGE_3: StageData = {
  stage: 3,
  title: '阶段三 · 循环',
  subtitle: 'for、while 与嵌套循环',
  summary:
    '前两个阶段的程序都是从上往下一口气执行完的。这个阶段我们让程序学会**重复**：\n\n' +
    '- 用 `for` 循环按次数重复（我知道要做 n 遍）；\n' +
    '- 用 `while` 循环按条件重复（我不知道要做几遍，只知道什么时候停）；\n' +
    '- 用 `break` 在找到答案时提前跳出循环。\n\n' +
    '学会**累加**、**累乘**、**嵌套循环画图**和**逐位拆解整数**以后，你就能写出真正"会干活"的程序了。\n\n' +
    '**提醒一句**：循环的代码看起来很短，但它会重复执行很多次，所以循环里每一行写错一点点，' +
    '结果就会差很远。写完一定要点「运行」，用题目给的样例亲手验证一遍。',
  problems: [
    lesson({
      id: 'j3-1',
      title: '从 1 加到 n',
      difficulty: '入门',
      knowledge: 'for 循环与累加',
      story:
        '如果让你算 1 + 2 + 3 + …… + 100，一个一个加当然能算出来，但太累了。\n\n' +
        '计算机最擅长做的，就是这种**重复很多次、又很有规律**的事情。' +
        '让程序重复执行一段代码的工具叫做**循环**，最常用的就是 `for` 循环。',
      task: '读入一个整数 `n`，计算 1 + 2 + 3 + … + n 的结果并输出。',
      lesson: `**\`for\` 循环长什么样**

\`\`\`java
for (int i = 1; i <= n; i++) {
    // 大括号里写要重复做的事情；i 每一轮的值都不一样
}
\`\`\`

读法：让变量 \`i\` 从 1 开始，只要 \`i\` 还小于等于 \`n\`，就把大括号里的代码再执行一遍。
圆括号里挤着三部分，用**英文分号** \`;\` 隔开（注意第 3 部分后面**没有**分号）：

${pointsTable([
  ['① 初始化 `int i = 1`', '造一个"计数器" `i`，让它从 1 开始数。整个循环开始前**只执行一次**'],
  ['② 条件 `i <= n`', '继续循环的条件：成立才再转一圈。**每一轮开始前**都要判断一次'],
  ['③ 步进 `i++`', '一圈走完以后把 `i` 加 1（`i++` 就等于 `i = i + 1;`）'],
])}

**执行顺序**（最容易记混，按这 5 步记）：

1. 执行 \`int i = 1\`，只做这一次；
2. 判断 \`i <= n\`：不成立，整个循环立刻结束，程序跳到 \`}\` 后面继续往下走；
3. 成立，就执行大括号里的循环体；
4. 执行 \`i++\`，计数器加 1；
5. 回到第 2 步继续判断——这样一圈一圈转，直到条件不成立为止。

一句话概括就是：**判断 → 执行 → 加 1 → 再判断**。循环体转了几圈，第 3 部分就执行了几次。

**"累加"就是准备一个存钱罐**

\`\`\`java
Scanner scanner = new Scanner(System.in);
int n = scanner.nextInt();

int sum = 0;                       // ① 存钱罐，初值必须是 0
for (int i = 1; i <= n; i++) {
    sum += i;                      // ② 把这一轮的 i 扔进去；sum += i 就是 sum = sum + i
}
System.out.println(sum);           // ③ 循环全部结束后才输出，只输出一次
\`\`\`

**跟着 \`n = 4\` 走一遍**：

| 第几轮 | \`i\` 的值 | 执行 \`sum += i\` 之后 \`sum\` 是多少 |
| --- | --- | --- |
| 1 | 1 | 1 |
| 2 | 2 | 3 |
| 3 | 3 | 6 |
| 4 | 4 | 10 |
| — | \`i\` 变成 5，\`5 <= 4\` 不成立 | 循环结束，输出 10 |

**要点**

${pointsTable([
  ['`i` 从 1 开始', '本题要加的数正好是 1…n 这 n 个数，所以从 1 开始最自然。从 0 开始会白加一个 0，这题碰巧还对，换个题目就是错的'],
  ['上界用 `<=`', '写成 `i < n` 只会加到 n - 1。输入 10 会得到 45，而正确答案是 55'],
  ['累加器初值必须是 0', 'Java 的规矩比 C++ 严：`int sum;` 不给初值就用会在**编译**阶段报错 `variable sum might not have been initialized`'],
  ['输出语句放在循环**外面**', '放进循环体会每加一次就打印一行，屏幕上出现一大片数字'],
  ['`i` 的作用范围', '`int i` 写在 `for` 的圆括号里，出了这个循环就不能再用它了'],
  ['循环体的大括号 `{}`', '循环体只有一句时可以不写大括号，但**永远写上**是个好习惯'],
])}`,
      inputFormat: '一行，一个整数 `n`（`0 ≤ n ≤ 1000`）。',
      outputFormat:
        '输出一个整数，表示 1 到 n 的和。当 `n = 0` 时和为 `0`（循环一次都不执行，`sum` 保持 0）。',
      mistakes: mistakesTable([
        ['`for (int i = 1; i < n; i++)`', '少加了一个数：输入 10 输出 45，正确答案是 55', '上界写 `i <= n`，把 n 也算进去'],
        ['`for (int i = 1; i <= n; )` 忘了 `i++`', '`i` 永远是 1，条件永远成立，程序停不下来（死循环）', '第 3 部分补上 `i++`'],
        ['`int sum;` 不给初值', '编译报错 `variable sum might not have been initialized`', '写成 `int sum = 0;`'],
        [
          '`System.out.println(sum);` 写在循环体里面',
          '每加一次就打印一行，输出了 n 行数字',
          '把它移到循环的 `}` 后面',
        ],
        [
          '三部分之间用逗号：`for (int i = 1, i <= n, i++)`',
          "报错 `';' expected`",
          '三部分之间必须是**英文分号**：`for (int i = 1; i <= n; i++)`',
        ],
        ['`System.out.println("sum");` 加了引号', '屏幕原样打印 `sum` 这 3 个字母', '去掉引号才是输出变量的值'],
      ]),
      tips:
        '- 先写 `int sum = 0;`，它就是装结果的"存钱罐"。\n' +
        '- 循环体里只写一句 `sum += i;`，其余都放在循环外面。\n' +
        '- 输入 `0` 时循环一次都不执行，`sum` 保持 0，正好输出 `0`，不用额外特判。\n' +
        '- 心里没底就点「运行」，用样例 `10 → 55` 验证一下。',
      starter: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        // TODO: 定义一个累加变量 sum，初值写成 0\n' +
          '        // TODO: 写 for 循环，让 i 从 1 一直走到 n（包含 n），每轮把 i 加到 sum 上\n' +
          '        // TODO: 循环结束后输出 sum',
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        int sum = 0;\n' +
          '        for (int i = 1; i <= n; i++) {\n' +
          '            sum += i;\n' +
          '        }\n' +
          '        System.out.println(sum);',
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '10\n', expected: '55\n' },
        { input: '1\n', expected: '1\n' },
        { input: '0\n', expected: '0\n' },
        { input: '1000\n', expected: '500500\n' },
      ],
      hints: [
        '先写 `int sum = 0;`，它就是装结果的"存钱罐"。',
        '循环写成 `for (int i = 1; i <= n; i++)`，循环体里写 `sum += i;`。',
        '`n = 0` 时循环一次都不执行，`sum` 保持 0，正好输出 0。',
        '输出的 `System.out.println(sum);` 要写在循环的 `}` 后面，不能写在循环体里。',
      ],
    }),

    lesson({
      id: 'j3-2',
      title: 'n 的阶乘',
      difficulty: '简单',
      knowledge: '累乘与 long',
      story:
        '`1 × 2 × 3 × … × n` 叫做 **n 的阶乘**，数学上记作 `n!`。比如 `5! = 1 × 2 × 3 × 4 × 5 = 120`。\n\n' +
        '要算阶乘，只要把上一题的"累加"换成"累乘"就行。不过这里藏着一个坑：' +
        '`int` 能存的最大整数只有 2147483647，而 `13! = 6227020800` 已经超过它了，' +
        '程序会算出一个莫名其妙的数字还不报错。',
      task: '读入一个整数 `n`，输出它的阶乘 `n!`。',
      lesson: `**循环骨架和上一题一模一样，只改两处：存钱罐的初值，和每一轮往里面放东西的动作。**

\`\`\`java
long result = 1;                 // ① 累乘器：初值必须是 1，不能是 0
for (int i = 1; i <= n; i++) {
    result *= i;                 // ② 把这一轮的 i 乘进去；result *= i 就是 result = result * i
}
System.out.println(result);      // ③ 循环结束后输出
\`\`\`

**为什么累加写 0、累乘要写 1？**

因为初值必须是"不改变结果"的那个数：

${pointsTable([
  ['累加从 0 开始', '0 加上任何数还是那个数，所以 `int sum = 0;` 是"空碗"'],
  ['累乘从 1 开始', '1 乘任何数还是那个数，所以 `long result = 1;`；写成 0 的话乘什么都得 0'],
  ['每轮的动作', '累加写 `sum += i;`，累乘写 `result *= i;`，只差一个符号'],
])}

**\`int\` 装不下 13!，要换成 \`long\`**

\`\`\`java
int bad = 1;
for (int i = 1; i <= 13; i++) {
    bad *= i;
}
System.out.println(bad);         // 屏幕上是一个奇怪的数，跟正确的 6227020800 完全不是一回事

long good = 1;
for (int i = 1; i <= 13; i++) {
    good *= i;
}
System.out.println(good);        // 6227020800，这才是对的
\`\`\`

装不下的后果**不是报错**，而是数字悄悄"溢出"成别的值，所以特别难发现。
\`long\` 是 Java 里"更能装"的整数类型，能存到大约 9.2 × 10¹⁸，本题到 20 都用不完。

${pointsTable([
  ['`int` 的容量', '最大 2147483647（约 21 亿）。算到 `12! = 479001600` 都还安全，从 `13!` 开始出错'],
  ['`long` 的容量', '最大 9223372036854775807（约 9.2 × 10¹⁸），本题最大只问到 20'],
  ['溢出不会报错', '算出来是个乱七八糟的数字，程序照样输出，靠肉眼看不出问题'],
  ['直接写大数字常量要加 `L`', '`long x = 6227020800L;`。不加 `L` 会报错 `integer number too large`，因为数字本身超了 `int` 范围'],
  ['`0! = 1`', '数学规定：一个数都不乘，结果是 1。循环 `for (int i = 1; i <= 0; ...)` 一次都不执行，`result` 保持 1，正好符合要求'],
  ['循环变量从 1 开始', '从 0 开始会多乘一个 0，结果永远是 0'],
  ['输出放在循环外面', '写在循环体里会输出 n 行中间结果'],
])}`,
      inputFormat: '一行，一个整数 `n`（`0 ≤ n ≤ 20`）。',
      outputFormat:
        '输出一个整数，表示 `n!`。数学上规定 `0! = 1`，所以输入 `0` 时要输出 `1`。',
      sample:
        '### 样例\n\n**输入**\n\n```\n5\n```\n\n**输出**\n\n```\n120\n```\n\n' +
        '再比如：输入 `0` 输出 `1`；输入 `1` 输出 `1`；输入 `20` 输出 `2432902008176640000`。\n\n' +
        '注意 `13! = 6227020800` 已经超出 `int` 的范围，所以累乘变量一定要用 `long`。',
      mistakes: mistakesTable([
        [
          '`int result = 1;`（类型用了 `int`）',
          '从 `13!` 开始输出乱七八糟的数，还不报错',
          '改用 `long result = 1;`',
        ],
        ['`long result = 0;`', '0 乘任何数都是 0，输出永远是 0', '累乘的初值必须是 1'],
        ['`for (int i = 0; i <= n; i++)`', '多乘了一个 0，结果永远 0', '让 `i` 从 1 开始'],
        ['把 `result *= i;` 写成 `result += i;`', '算出来是求和不是求积（`5` 会输出 `15`）', '累乘用 `*=`'],
        ['`System.out.println(result);` 写在循环体里', '输出 n 行中间结果', '移到循环的 `}` 后面'],
        [
          '`long x = 6227020800;` 不加 `L`',
          '报错 `integer number too large`',
          '写成 `6227020800L`；或者干脆让程序自己乘出来，不手写这个常量',
        ],
      ]),
      tips:
        '- 累乘器和累加器长得像，只有初值和运算符不一样：`long result = 1;` + `result *= i;`。\n' +
        '- 输入 `0` 和 `1` 都输出 `1`，可以用它们检查初值是不是写对了（写 0 的话这里就会露馅）。\n' +
        '- 题目最多问到 20，因为 `20!` 已经接近 `long` 的上限。',
      starter: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        // TODO: 定义一个 long 变量 result 当累乘器，初值写成 1（不能写 0）\n' +
          '        // TODO: 写 for 循环，让 i 从 1 一直走到 n，每轮把 result 乘上 i\n' +
          '        // TODO: 循环结束后输出 result',
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        long result = 1;\n' +
          '        for (int i = 1; i <= n; i++) {\n' +
          '            result *= i;\n' +
          '        }\n' +
          '        System.out.println(result);',
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '5\n', expected: '120\n' },
        { input: '0\n', expected: '1\n' },
        { input: '13\n', expected: '6227020800\n' },
        { input: '20\n', expected: '2432902008176640000\n' },
      ],
      hints: [
        '累乘器和累加器不一样：`long result = 1;`，初值是 1 而不是 0。',
        '循环体里写 `result *= i;`（等于 `result = result * i;`）。',
        '输入 13 的结果 6227020800 已经超出 `int` 范围，所以变量必须用 `long`。',
        '输入 0 时循环一次都不执行，`result` 保持 1，正好满足 `0! = 1`。',
      ],
    }),

    lesson({
      id: 'j3-3',
      title: '打印直角三角形',
      difficulty: '简单',
      knowledge: '嵌套循环打印图形',
      story:
        '打印图形是练习**循环嵌套**（一个循环里面再写一个循环）的经典题目。\n\n' +
        '以前我们一次只打印一行，现在要在程序里"一行一行地画"——' +
        '这正是嵌套循环最擅长的事：外层管行，内层管这一行里的每个符号。',
      task:
        '读入一个整数 `n`，打印一个 n 行的直角三角形：第 1 行 1 个星号，第 2 行 2 个星号……' +
        '第 n 行 n 个星号，所有星号都从行首开始排（左对齐）。',
      lesson: `**嵌套循环**就是"循环里面再套一个循环"。它的规矩只有一条，但极其重要：

> **外层循环走一步，内层循环就要从头到尾走完一整圈。**

就像钟表：分针转一整圈（内层跑完），时针才走一格（外层走一步）。

${pointsTable([
  ['外层循环', '管"现在是第几行"。它每走一步，就代表开始画新的一行'],
  ['内层循环', '管"这一行要打印几个符号"。它从 1 数到这一行需要的个数'],
  ['换行语句', '属于外层循环：内层跑完一整圈、一行打完了，才换行一次'],
])}

**本题的骨架**

\`\`\`java
for (int i = 1; i <= n; i++) {          // 外层：现在是第 i 行
    for (int j = 1; j <= i; j++) {      // 内层：第 i 行要打印 i 个星号
        System.out.print("*");          // 打印一个星号，print 不换行
    }
    System.out.println();               // 这一行打完才换行（在内层循环外面！）
}
\`\`\`

**跟着 \`n = 3\` 走一遍**：

| 外层第几轮 | 内层 \`j\` 的取值 | 这一行打印出来 |
| --- | --- | --- |
| \`i = 1\` | 1 | \`*\` |
| \`i = 2\` | 1, 2 | \`**\` |
| \`i = 3\` | 1, 2, 3 | \`***\` |

注意内层的条件写的是 **\`i\`**，不是 \`n\`：第 1 行内层只转 1 圈，第 2 行转 2 圈……所以行号越大星号越多，画出来才是三角形。写成 \`j <= n\` 的话每行都是 n 个星号，就变成一个正方形了。

**\`print\` 和 \`println\` 的区别是这题的关键**

${pointsTable([
  ['`System.out.print("*")`', '打印星号但**不换行**，后面的内容紧跟在同一行。本题的星号用它'],
  ['`System.out.println("*")`', '打印星号**并且换行**，一个星号就占整整一行。用错了图案会竖着排'],
  ['`System.out.println()`', '圆括号里什么都不写，只输出一个换行，用来给一行收尾'],
])}

**要点**

${pointsTable([
  ['内层条件用 `i`', '用 `n` 就打印不出三角形，只会得到一个矩形'],
  ['换行放对内层外面', '`System.out.println();` 要写在**内层循环的 `}` 之后、外层循环的 `}` 之前**'],
  ['内外层变量名不能相同', '两层都写 `int i` 会报错 `variable i is already defined`，内层习惯换个名字写 `j`'],
  ['星号后面不加空格', '写 `print("*")`；写成 `print("* ")` 每行末尾会多出空格，与期望输出不一致'],
  ['大括号要配对', '两层循环一共两对 `{}`，少一个就编译不过'],
  ['行数由外层决定', '外层转 n 圈就是 n 行。输入 1 时也要正常输出一行 `*`'],
])}`,
      inputFormat: '一行，一个整数 `n`（`1 ≤ n ≤ 20`）。',
      outputFormat:
        '输出 `n` 行，第 `i` 行有 `i` 个星号 `*`。每行末尾都要换行，行首和行末都**不要**多输出空格。',
      mistakes: mistakesTable([
        [
          '内层用了 `System.out.println("*");`',
          '每个星号占一行，图案竖着排成了"楼梯"',
          '内层用 `System.out.print("*");`（不带 ln）',
        ],
        ['内层写成 `for (int j = 1; j <= n; j++)`', '每行都是 n 个星号，变成正方形', '条件用 `j <= i`'],
        [
          '换行写在内层循环体里',
          '每打印一个星号就换一次行，图案全散了',
          '把换行移到内层循环的 `}` 后面',
        ],
        ['两层都写 `for (int i = ...)`', '报错 `variable i is already defined in method main`', '内层换成 `j`'],
        ['忘了写换行', '所有星号挤在同一行，只有 1 行输出', '每行打完补一句 `System.out.println();`'],
        ['星号后面多打了空格', '行末有空格，与期望输出不一致', '只输出 `*` 本身'],
      ]),
      tips:
        '- 两层循环要分开想：外层管行数，内层管这一行打印几个。\n' +
          '- 内层循环的条件是 `j <= i`，用 `i` 才能让每行的星号逐行增加。\n' +
          '- 换行的 `System.out.println();` 要放在内层循环**外面**、外层循环**里面**。\n' +
          '- 输入 `1` 应该只输出一行 `*`，可以用它检查两层循环的边界。',
      starter: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        // TODO: 外层 for 循环让 i 从 1 走到 n，i 表示"第几行"（也是这一行的星号个数）\n' +
          '        // TODO: 内层 for 循环每行打印 i 个星号，注意用 print（不要用 println）\n' +
          '        // TODO: 每行打印完之后用 System.out.println(); 换行',
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        for (int i = 1; i <= n; i++) {\n' +
          '            for (int j = 1; j <= i; j++) {\n' +
          '                System.out.print("*");\n' +
          '            }\n' +
          '            System.out.println();\n' +
          '        }',
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '3\n', expected: '*\n**\n***\n' },
        { input: '1\n', expected: '*\n' },
        { input: '5\n', expected: '*\n**\n***\n****\n*****\n' },
        {
          input: '20\n',
          expected:
            '*\n**\n***\n****\n*****\n******\n*******\n********\n*********\n**********\n' +
            '***********\n************\n*************\n**************\n***************\n' +
            '****************\n*****************\n******************\n*******************\n********************\n',
        },
      ],
      hints: [
        '两层循环分开想：外层管行数，内层管这一行打印几个星号。',
        '内层循环的条件是 `j <= i`，用 `i` 才能让每一行的星号个数逐行增加。',
        '打印星号要用 `System.out.print("*")`，`println` 会让每个星号单独占一行。',
        '换行的 `System.out.println();` 放在内层循环的 `}` 之后、外层循环的 `}` 之前。',
      ],
    }),

    lesson({
      id: 'j3-4',
      title: '九九乘法表',
      difficulty: '中等',
      knowledge: '嵌套循环与行末空格',
      story:
        '九九乘法表大家都背过，现在让程序把它一行一行打出来。\n\n' +
        '它的循环结构和上一题打印三角形**几乎一样**，区别只是把"打印一个星号"换成了"打印一个算式"。' +
        '多出来的新问题是：算式之间要有空格，但**行末不能多出空格**——' +
        '评测是逐字符比对的，末尾多一个空格就直接算错。',
      task:
        '读入一个整数 `n`，输出乘法表的前 `n` 行。第 `i` 行从左到右依次输出 `j = 1, 2, …, i` 的算式，' +
        '每个算式写成 `i*j=积`（乘号两边、等号两边都不加空格），算式之间用**一个空格**分隔，行末不要有多余空格。',
      lesson: `骨架和上一题打印三角形**完全一样**，分工也照旧：

${pointsTable([
  ['外层 `i`', '管"第几行"。第 `i` 行里一共有 `i` 个算式'],
  ['内层 `j`', '管"这一行里的第几个算式"，从 1 数到 `i`'],
  ['换行', '一行全部输出完才换行，所以放在内层循环外面、外层循环里面'],
])}

**怎么输出一个算式？**

\`\`\`java
System.out.print(i + "*" + j + "=" + (i * j));    // i=3、j=2 时，屏幕上出现 3*2=6
\`\`\`

拆开读：

${pointsTable([
  ['`i`', '数字变量，输出它的值（不是字母 i）'],
  ['`+ "*" +`', '用 `+` 把数字和文字"粘"起来。加引号的 `"*"` 才是文字乘号'],
  ['`"*"`', '引号里的内容原样显示，电脑不会去算它'],
  ['`(i * j)`', '写在圆括号里表示"先算乘法、再把结果拼上去"。不加括号写成 `i * j` 也对（`*` 的优先级更高），但加括号一眼就能看懂'],
  ['`System.out.print`', '算式之间不能换行，所以内层用 `print` 而不是 `println`'],
])}

对比一下几种写法，结果差别很大：

| 写法 | 屏幕上出现 | 说明 |
| --- | --- | --- |
| \`System.out.print(i * j);\` | \`6\` | 只算出来、只输出结果，看不到算式 |
| \`System.out.print("i*j");\` | \`i*j\` | 引号里的内容原样显示，电脑不会去算它 |
| \`System.out.print(i + "*" + j + "=" + (i * j));\` | \`3*2=6\` | 本题要的写法 |

**本题最容易踩的坑：行末空格**

如果每输出一个算式就在后面补一个空格，那么每行最后一个算式的后面也会拖一个空格：

\`\`\`text
1*1=1·          ← 这个 · 就是多出来的空格，评测会判错
2*1=2·2*2=4·
\`\`\`

小技巧是**把空格加在算式的前面，而且第一个算式不加**：

\`\`\`java
for (int j = 1; j <= i; j++) {
    if (j > 1) {
        System.out.print(" ");          // 只有第二个及以后的算式，前面才补一个空格
    }
    System.out.print(i + "*" + j + "=" + (i * j));   // 然后输出算式本身
}
System.out.println();                   // 这一行全部输出完才换行
\`\`\`

这样一来，空格永远只夹在两个算式**中间**：第一个算式前面没有空格，最后一个算式后面也没有空格，行末干干净净。

**要点**

${pointsTable([
  ['内层上界是 `i`', '第 `i` 行只有 `i` 个算式，不是 `n` 个'],
  ['空格加在算式**前**面', '只在 `j > 1` 时先输出一个空格，行末就不会多空格'],
  ['乘号是字符串 `"*"`', '带引号才是文字乘号；不带引号的 `*` 是乘法运算符，不能单独输出'],
  ['乘积不加引号', '`(i * j)` 会算出结果；写成 `"(i * j)"` 只会原样显示这几个字符'],
  ['换行的位置', '`System.out.println();` 放在内层循环外面、外层循环里面'],
  ['内层变量不能叫 `i`', '和外层重名会报错，习惯用 `j`'],
])}`,
      inputFormat: '一行，一个整数 `n`（`1 ≤ n ≤ 9`）。',
      outputFormat: '输出 `n` 行，每行末尾换行，**行末不要有多余空格**。',
      sample:
        '### 样例\n\n**输入**\n\n```\n3\n```\n\n**输出**\n\n```\n1*1=1\n2*1=2 2*2=4\n3*1=3 3*2=6 3*3=9\n```\n\n' +
        '注意第 3 行末尾是 `3*3=9`，**后面没有空格**；算式之间（`2*2=4` 前面）恰好一个空格。',
      mistakes: mistakesTable([
        [
          '每个算式后面都补一个空格（`print(i + "*" + j + "=" + (i * j) + " ");`）',
          '行末多出一个空格，评测逐字符比对，直接判错',
          '改成只在 `j > 1` 时**先**输出一个空格',
        ],
        ['内层写成 `for (int j = 1; j <= n; j++)`', '每行都输出 n 个算式，成了矩形而不是三角', '条件用 `j <= i`'],
        ['内层用 `println` 输出算式', '一行只放得下一个算式，表格被拉成 n 行', '内层用 `print`，换行留给外层'],
        ['写成 `print("i*j=i*j");`', '原样打印 `i*j=i*j`，一个数都没算', '数字变量不加引号：`i + "*" + j + "=" + (i * j)`'],
        ['忘了最后换行', '所有内容拼在一行，只有 1 行输出', '内层循环后面补 `System.out.println();`'],
        ['算式里加了空格（`i + " * " + j + ...`）', '输出 `1 * 1 = 1`，与期望的 `1*1=1` 不一致', '乘号和等号两边都不加空格'],
      ]),
      tips:
        '- 外层 `i` 从 1 到 `n`，内层 `j` 从 1 到 `i`，和上一题打印三角形完全一样。\n' +
          '- 输出一个算式：`System.out.print(i + "*" + j + "=" + (i * j));`。\n' +
          '- 空格别加在算式后面（行末会多出来），可以改成 `if (j > 1) { System.out.print(" "); }` 加在前面。\n' +
          '- 输入 `1` 时输出只有一行 `1*1=1`，且末尾**没有**空格，可以用它验证空格技巧。',
      starter: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        // TODO: 外层 for 循环让 i 从 1 走到 n，表示第 i 行\n' +
          '        // TODO: 内层 for 循环让 j 从 1 走到 i，按 i*j=积 的格式输出算式\n' +
          '        // TODO: 算式之间输出一个空格，注意行末不要多输出空格\n' +
          '        // TODO: 每行结束后换行',
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        for (int i = 1; i <= n; i++) {\n' +
          '            for (int j = 1; j <= i; j++) {\n' +
          '                if (j > 1) {\n' +
          '                    System.out.print(" ");\n' +
          '                }\n' +
          '                System.out.print(i + "*" + j + "=" + (i * j));\n' +
          '            }\n' +
          '            System.out.println();\n' +
          '        }',
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '3\n', expected: '1*1=1\n2*1=2 2*2=4\n3*1=3 3*2=6 3*3=9\n' },
        { input: '1\n', expected: '1*1=1\n' },
        {
          input: '5\n',
          expected:
            '1*1=1\n2*1=2 2*2=4\n3*1=3 3*2=6 3*3=9\n' +
            '4*1=4 4*2=8 4*3=12 4*4=16\n5*1=5 5*2=10 5*3=15 5*4=20 5*5=25\n',
        },
        {
          input: '9\n',
          expected:
            '1*1=1\n2*1=2 2*2=4\n3*1=3 3*2=6 3*3=9\n' +
            '4*1=4 4*2=8 4*3=12 4*4=16\n5*1=5 5*2=10 5*3=15 5*4=20 5*5=25\n' +
            '6*1=6 6*2=12 6*3=18 6*4=24 6*5=30 6*6=36\n' +
            '7*1=7 7*2=14 7*3=21 7*4=28 7*5=35 7*6=42 7*7=49\n' +
            '8*1=8 8*2=16 8*3=24 8*4=32 8*5=40 8*6=48 8*7=56 8*8=64\n' +
            '9*1=9 9*2=18 9*3=27 9*4=36 9*5=45 9*6=54 9*7=63 9*8=72 9*9=81\n',
        },
      ],
      hints: [
        '骨架和上一题一样：外层 `i` 从 1 到 `n`，内层 `j` 从 1 到 `i`。',
        '一个算式这样输出：`System.out.print(i + "*" + j + "=" + (i * j));`。',
        '空格不能无脑加在算式后面（那样行末会多一个空格），可以只在 `j > 1` 时先输出一个空格。',
        '每行结束后要换行：`System.out.println();` 放在内层循环的 `}` 后面。',
      ],
    }),

    lesson({
      id: 'j3-5',
      title: '数位之和',
      difficulty: '简单',
      knowledge: 'while 与数位拆解',
      story:
        '整数在程序里可以像积木一样拆开：\n\n' +
        '- 取出一个整数的**最后一位**：用 `n % 10`（对 10 取余数）；\n' +
        '- 把最后一位**扔掉**：用 `n /= 10`（除以 10，整数除法会丢掉小数部分）。\n\n' +
        '拿 `n = 12345` 举例，反复做这两步，就能把每一位数字一个一个"掏"出来。',
      task: '读入一个非负整数 `n`，求它各个数位上的数字之和。',
      lesson: `**先看两位"剪刀"**

${pointsTable([
  ['`n % 10`', '取**最后一位**。`%` 是取余数，而一个整数除以 10 的余数正好就是末位数字。`12345 % 10` 得到 `5`'],
  ['`n /= 10`', '**扔掉最后一位**。整数除法会自动丢掉小数部分：`12345 / 10` 得到 `1234`'],
  ['配合使用', '先用 `% 10` 取出末位，再用 `/ 10` 把它扔掉，剩下的数就短了一位'],
])}

**跟着 \`n = 12345\` 拆一遍**

| 第几轮 | \`n % 10\` 取出的数字 | 做完 \`n /= 10\` 之后的 \`n\` |
| --- | --- | --- |
| 1 | 5 | 1234 |
| 2 | 4 | 123 |
| 3 | 3 | 12 |
| 4 | 2 | 1 |
| 5 | 1 | 0 |

\`n\` 变成 0 就没得拆了，循环自然结束。

**要拆几轮事先并不知道**（得看 \`n\` 有几位），这种"条件成立就一直做"的场合，用 \`while\` 循环最合适：

\`\`\`java
while (n > 0) {
    // 只要 n 还是正数，就一直重复这里
}
\`\`\`

\`while\` 和 \`for\` 最大的区别是：**先判断，后执行**。先看条件，成立才进去做；条件一开始就不成立，循环体**一次都不执行**。\`for\` 适合"次数事先就知道"的场合（比如 1 加到 n），\`while\` 适合"不知道要转几圈、只知道什么情况下该继续"的场合。

**完整的程序**

\`\`\`java
int sum = 0;              // ① 累加器：装数位之和，初值 0
while (n > 0) {           // ② 先判断：n 还有位没拆完吗？
    sum += n % 10;        // ③ 取出最后一位，加进 sum
    n /= 10;              // ④ 把最后一位扔掉
}
System.out.println(sum);  // ⑤ 循环结束后输出
\`\`\`

**注意第 ③④ 两步的顺序是"先取位、后缩位"**。反过来的话，最右边那一位还没被加进 \`sum\` 就被丢掉了，结果会少算一位。

**要点**

${pointsTable([
  ['先判断后执行', '条件不成立时循环体一次也不做——这正是输入 `0` 时能正确输出 `0` 的原因，不需要额外特判'],
  ['循环里必须让 `n` 变小', '每轮都要写 `n /= 10;`，否则 `n > 0` 永远成立，程序停不下来（死循环）'],
  ['`%` 和 `/` 别写反', '取末位用 `% 10`，扔掉末位用 `/ 10`'],
  ['累加器初值 0', '写成 `int sum = 0;`。Java 里不给初值直接用会编译报错'],
  ['输出放在循环外', '写在循环里会输出每一步的中间结果，出现好几行数字'],
  ['`n` 会被改掉', '循环结束时 `n` 已经变成 0 了。如果还要用原来的 n，得提前用另一个变量存起来'],
])}`,
      inputFormat: '一行，一个非负整数 `n`（`0 ≤ n ≤ 1000000000`）。',
      outputFormat: '输出一个整数，表示 `n` 各个数位上的数字之和。当 `n = 0` 时输出 `0`。',
      sample:
        '### 样例\n\n**输入**\n\n```\n12345\n```\n\n**输出**\n\n```\n15\n```\n\n' +
        '1 + 2 + 3 + 4 + 5 = 15。再比如输入 `0` 输出 `0`；输入 `999999999` 输出 `81`；' +
        '输入 `1000000000` 输出 `1`（只算非零的那一位）。',
      mistakes: mistakesTable([
        ['忘了写 `n /= 10;`', '`n` 永远不变、条件永远成立，程序停不下来（死循环）', '循环体里必须让 `n` 变小'],
        [
          '先写 `n /= 10;` 再写 `sum += n % 10;`',
          '最右边那一位被丢掉了：输入 `12345` 只得到 10',
          '顺序必须是**先**取位、**后**缩位',
        ],
        ['写成 `sum += n / 10;`（把 `%` 写成了 `/`）', '加进去的是"去掉末位后的数"，结果离谱', '取末位用 `% 10`'],
        ['`while (n >= 0)`', '`n` 变成 0 后条件仍然成立，死循环', '条件写 `n > 0`'],
        ['`int sum;` 不给初值', '编译报错 `variable sum might not have been initialized`', '写成 `int sum = 0;`'],
        ['`System.out.println(sum);` 写在循环里', '会输出好几行中间结果', '移到循环的 `}` 后面'],
      ]),
      tips:
        '- 取最后一位用 `n % 10`，去掉最后一位用 `n /= 10`。\n' +
          '- 循环体里必须**先**累加、**后**缩小，顺序反了会少算一位。\n' +
          '- 输入 `0` 时 `while (n > 0)` 一次都不执行，直接输出 0，正好是对的。\n' +
          '- 这套"取末位 + 缩位"的写法以后判断回文数、数字倒序也都能用。',
      starter: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        // TODO: 定义一个变量 sum 当数位之和的累加器，初值写成 0\n' +
          '        // TODO: 用 while (n > 0) 循环：每轮先用 n % 10 取出最后一位加到 sum 上\n' +
          '        // TODO: 再用 n /= 10 去掉最后一位，直到 n 变成 0\n' +
          '        // TODO: 循环结束后输出 sum',
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        int sum = 0;\n' +
          '        while (n > 0) {\n' +
          '            sum += n % 10;\n' +
          '            n /= 10;\n' +
          '        }\n' +
          '        System.out.println(sum);',
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '12345\n', expected: '15\n' },
        { input: '0\n', expected: '0\n' },
        { input: '1000000000\n', expected: '1\n' },
        { input: '999999999\n', expected: '81\n' },
      ],
      hints: [
        '取最后一位用 `n % 10`，去掉最后一位用 `n /= 10`。',
        '循环体里必须**先**累加、**后**缩小，顺序反了会少算一位。',
        '循环条件是 `while (n > 0)`；输入 0 时一次都不执行，直接输出 0，正好是对的。',
      ],
    }),

    lesson({
      id: 'j3-6',
      title: '判断素数',
      difficulty: '中等',
      knowledge: '标志变量与 break',
      story:
        '**素数**（也叫质数）是指**大于 1**、并且只能被 1 和它自己整除的整数。\n\n' +
        '比如 2、3、5、7、11 都是素数，而 4、6、9 不是。\n\n' +
        '要判断一个数是不是素数，最直接的办法就是：从 2 开始一个个试着除。' +
        '只要发现有一个数能整除它，就可以立刻下结论"不是素数"，后面的数不用再试了。',
      task: '读入一个整数 `n`，判断它是不是素数，输出 `是素数` 或 `不是素数`。',
      lesson: `把人话翻译成代码，一共三件事。

**① 标志变量：门口挂的一块牌子**

\`\`\`java
boolean isPrime = true;    // 先假设它是素数
\`\`\`

\`boolean\` 是 Java 里专门装"真 / 假"两个值的类型，只能写 \`true\` 或 \`false\`。
（注意：C++ 里这个类型叫 \`bool\`，Java 里必须写成 **\`boolean\`**。）

先挂上"是素数"的牌子，然后去找**反例**：一旦发现反例就翻个面，取值为 \`false\`。
最后只看牌子决定输出哪句话：

\`\`\`java
if (isPrime) {
    System.out.println("是素数");
} else {
    System.out.println("不是素数");
}
\`\`\`

**② \`break\`：提前结束循环**

程序执行到 \`break;\` 时，立刻跳出**当前这一层**循环，跑到循环的 \`}\` 后面接着往下走。
像在一排抽屉里找钥匙：只要找到了，后面的抽屉就不用再翻。

本题只有一层循环，跳出来正好就到了输出的那一步：

\`\`\`java
for (int i = 2; i * i <= n; i++) {
    if (n % i == 0) {      // 发现能整除 → 说明还有别的因数
        isPrime = false;   // 翻牌子
        break;             // 后面的数不用再试
    }
}
\`\`\`

**③ 试除到哪为止？不用试到 \`n - 1\`**

道理很简单：如果 \`n = a × b\` 而且 \`a ≤ b\`，那么小的那个因数 \`a\` 一定不超过 \`√n\`。
所以只要在 2 到 \`√n\` 之间找不到因数，更大的那一半也肯定找不到。
代码里写成 \`i * i <= n\`，等价于 \`i <= √n\`，比调用 \`sqrt\` 方便，也不用担心小数误差。

**把三件事拼起来**

\`\`\`java
boolean isPrime = true;                    // ① 先假设它是素数
if (n < 2) {
    isPrime = false;                       // ② 1 和 0 都不是素数，单独处理
} else {
    for (int i = 2; i * i <= n; i++) {     // ③ 只试到 √n 就够了
        if (n % i == 0) {                  // ④ 发现能整除
            isPrime = false;               // ⑤ 翻牌子
            break;                         // ⑥ 跳出循环
        }
    }
}
if (isPrime) {
    System.out.println("是素数");
} else {
    System.out.println("不是素数");
}
\`\`\`

**跟着两个例子走一遍**

- \`n = 15\`：\`i = 2\` 时 \`15 % 2 = 1\`，除不尽；\`i = 3\` 时 \`15 % 3 = 0\` → 翻牌子 + \`break\`，输出「不是素数」。
- \`n = 17\`：\`i\` 一直试到 4（因为 \`4 * 4 = 16 <= 17\` 成立）都除不尽；\`i\` 变成 5 时 \`5 * 5 = 25 > 17\`，循环自然结束，牌子还是 \`true\`，输出「是素数」。

**要点**

${pointsTable([
  ['标志初值写 `true`', '先假设"是素数"，然后去找反例；写反了结论就全反'],
  ['试除从 2 开始', '从 1 开始的话 `n % 1 == 0` 永远成立，所有数都会被判成不是素数'],
  ['条件写 `i * i <= n`', '等价于 `i <= √n`。写成 `i * i < n` 时，9、25 这类平方数会被误判成素数'],
  ['`n < 2` 必须特判', '1 不是素数，可它的循环一次都不执行、牌子仍是 `true`，不特判就会错判'],
  ['`break` 写在 `if` 里面', '只有"发现能整除"这一种情况才跳出循环'],
  ['输出用 `if / else`', '直接 `System.out.println(isPrime);` 只会打印 `true` / `false`，不是题目要的中文'],
])}`,
      inputFormat: '一行，一个整数 `n`（`0 ≤ n ≤ 10000`）。',
      outputFormat: '如果 `n` 是素数输出 `是素数`，否则输出 `不是素数`（每行末尾换行）。',
      sample:
        '### 样例\n\n**输入**\n\n```\n17\n```\n\n**输出**\n\n```\n是素数\n```\n\n' +
        '再比如：输入 `1` 输出 `不是素数`（1 不是素数）；输入 `2` 输出 `是素数`（2 是最小的素数）；' +
        '输入 `9` 输出 `不是素数`（9 = 3 × 3，是个平方数）。',
      mistakes: mistakesTable([
        ['用 C++ 的写法 `bool isPrime = true;`', '报错 `cannot find symbol: class bool`', 'Java 里是 `boolean isPrime = true;`'],
        ['标志初值写成 `false`', '所有数都被判成"不是素数"，结论全反', '初值写 `true`，找到反例才改成 `false`'],
        ['`for (int i = 1; i * i <= n; i++)`', '`n % 1 == 0` 永远成立，所有数都不是素数', '从 `i = 2` 开始试'],
        ['条件写 `i * i < n`', '9、25、49 这类平方数被误判成素数', '写成 `i * i <= n`（取到 √n 本身）'],
        ['忘了特判 `n < 2`', '输入 1 输出「是素数」（循环一次都不执行，牌子还是 true）', '先判断 `if (n < 2) { isPrime = false; }`'],
        ['`System.out.println(isPrime);`', '输出 `true` 或 `false`，不是题目的中文', '用 `if (isPrime) { ... } else { ... }` 分别输出'],
      ]),
      tips:
        '- 标志变量写法：`boolean isPrime = true;`，只要找到反例就改成 `false`。\n' +
          '- 循环可以写成 `for (int i = 2; i * i <= n; i++)`，一旦 `n % i == 0` 就 `break;`。\n' +
          '- 别忘了 1 不是素数，需要单独判断 `n < 2`。\n' +
          '- 输出是中文的「是素数」/「不是素数」，建议从题面复制，别用输入法打错字。',
      starter: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        // TODO: 定义一个 boolean 标志变量 isPrime，一开始先假设它是素数\n' +
          '        // TODO: 单独处理 n < 2 的情况（0 和 1 都不是素数）\n' +
          '        // TODO: 用 for 循环检查 2 到 sqrt(n)（也就是 i * i <= n）之间有没有能整除 n 的数\n' +
          '        // TODO: 一旦发现（n % i == 0），把标志改成 false 并 break 跳出循环\n' +
          '        // TODO: 根据 isPrime 输出「是素数」或「不是素数」',
        { imports: 'import java.util.Scanner;' },
      ),
      solution: mainOnly(
        '        Scanner scanner = new Scanner(System.in);\n' +
          '        int n = scanner.nextInt();\n' +
          '        boolean isPrime = true;\n' +
          '        if (n < 2) {\n' +
          '            isPrime = false;\n' +
          '        } else {\n' +
          '            for (int i = 2; i * i <= n; i++) {\n' +
          '                if (n % i == 0) {\n' +
          '                    isPrime = false;\n' +
          '                    break;\n' +
          '                }\n' +
          '            }\n' +
          '        }\n' +
          '        if (isPrime) {\n' +
          '            System.out.println("是素数");\n' +
          '        } else {\n' +
          '            System.out.println("不是素数");\n' +
          '        }',
        { imports: 'import java.util.Scanner;' },
      ),
      tests: [
        { input: '17\n', expected: '是素数\n' },
        { input: '2\n', expected: '是素数\n' },
        { input: '1\n', expected: '不是素数\n' },
        { input: '9\n', expected: '不是素数\n' },
      ],
      hints: [
        '标志变量写法：`boolean isPrime = true;`，只要找到反例就改成 `false`。',
        '循环可以写成 `for (int i = 2; i * i <= n; i++)`，一旦 `n % i == 0` 就 `isPrime = false; break;`。',
        '别忘了 1 不是素数，需要单独判断 `n < 2`。',
        '输出是中文的「是素数」/「不是素数」，用 `if (isPrime) { ... } else { ... }` 分别输出。',
      ],
    }),
  ],
};
